# 実装指示書: ドラッグ指示UI＋経由点システム

対象: `index.html`（AFBE）
前提: `CLAUDE.md`と`AFBE_システム設計書.md`を先に読むこと。本書は両文書に対する**追加仕様**であり、矛盾があれば本書が新しい（実装完了後、本書の内容を設計書§4とCLAUDE.mdに反映すること）。
原則: 仕様が曖昧な場合は推測せずUCに確認。フェイズ単位で実機検証→UC確認。

> **[2026-09-15訂正]** Block A実装後にUCへ確認したところ、本書作成時の想定と食い違いが判明した。
> - **指示パネルのチップUI（4種ボタン）は完全廃止**。指示を出す手段はドラッグのみにする（B-1の「チップUIは残す」は撤回）
> - **「rush」（敵拠点への自動追尾）という仕組み自体が不要**。敵拠点近くへの矢印は単なる`goto`として扱い、到達したら「守る」化＝射程内なら自動攻撃、という既存のgoto到達ロジックだけで十分（A-4で追加した「rush+経由点」は丸ごと不要になった）
> - 上記の削除・置き換えはBlock B（ドラッグUI本体）実装のタイミングでまとめて行う方針（UC了承済み、2026-09-15）。Block A時点のコードにはまだ`rush`とA-4の経由点対応が残っているが、これはBlock Bで除去される暫定状態
> - 以下の本文（B-1・A-4含む）は訂正前の原文のまま残す（経緯の記録として）。実装時は上記の訂正を優先すること
>
> **[2026-09-15] Block B実装完了**。上記訂正の通り、チップUI（`PURPOSES`・`.purpose-chip`）とrush（`computeGoal`のrush分岐・`advanceToNextRound`のrush上書き）はコードから完全に削除済み。B-2の「敵拠点」行は「空きマス」に統合され`goto`扱いになった。B-1〜B-3（ドラッグ検出・曲がり角抽出・終点解釈）は実装・実機検証済み。B-4（モバイル対応）は`touch-action:none`とPointer Events採用で対応したが、タッチ実機での動作確認は未実施（UC確認が必要）。詳細はCLAUDE.md「ドラッグ指示UI（Block B）」の項を参照。**次はBlock C（線の表示3層）**

---

## 0. 目的と全体像

配置フェイズの指示出しを「キャラをタップ→パネルのチップを選ぶ」から「**キャラからドラッグして線を引く**」に一本化する（イナズマイレブン方式）。1体あたりの操作を1ドラッグにし、15体規模でも作業感なく指示を出せるようにする。あわせて、なぞった線の曲がり角を**経由点**として採用し、「裏道を通って拠点へ」のような精密な進軍指示を可能にする。

3つの実装ブロックに分ける:

- **A. 経由点システム**（データモデル＋戦闘シミュレータ。UIなしでテスト可能にする）
- **B. ドラッグ指示UI**（配置フェイズの入力系）
- **C. 線の表示3層**（描画系）

A→B→Cの順に実装し、各ブロック完了時にUCへ確認する。

---

## A. 経由点システム

### A-1. データモデル

- ユニットの `targetHex`（単一マス）を `waypoints`（マスの配列 `[{col,row}, ...]`）に置き換える。**配列の最終要素が最終目的地**、それ以外が経由点
- `purpose === "goto"` のとき必ず `waypoints` を持つ（最低1要素）。それ以外のpurposeでは原則null（例外: A-4の「拠点へ＋経由点」）
- **後方互換**: Firebaseや復元処理で旧形式 `targetHex` を読んだ場合は `waypoints = [targetHex]` に変換する（`restoreMyPlacement()`・`tryAutoRejoin()`経由の古いデータ対策）

### A-2. シミュレータの移動処理（goto）

tickごと、gotoユニットの移動先決定は以下の手順:

1. **経由点の消化判定**: 自分の現在地が `waypoints[0]`（先頭の経由点）と同一マス**または隣接**なら、その経由点を消化（配列から除去）。連続で条件を満たす限り繰り返し消化する。ただし**最終要素（最終目的地）だけは隣接では消化しない**（実マスに到達した時のみ。到達時の挙動は従来のgoto到達と同じ＝purposeを"guard"に自動変更、waypointsは空にせず最終目的地を残す ※C-4と§goto到達→rush化の既存ロジック維持のため）
2. **移動目標の選定**: `waypoints` を先頭から順に見て、**経路探索（BFS）で経路が引ける最初の要素**をそのtickの移動目標とし、1マス移動する
3. どの要素にも経路が引けなければ**停止**（行動力消費なし。既存の停止と同じ扱い）

注意点:

- 手順2は「一時的に塞がれた経由点を**そのtickだけ飛ばす**」処理であり、経由点を配列から**削除しない**。次のtickで封鎖が解けていれば元の経由点に戻る。恒久的な消化は手順1（隣接到達）のみ。これにより「キャラが一瞬横切っただけで経由点が捨てられる」ことを防ぐ
- 全処理は既存の決定論の枠内（追加の乱数は使わない）
- イベントログの追加は不要（移動は既存のmoveイベントのまま。経由点消化はログに出さなくてよい）

### A-3. ラウンド跨ぎ

- 残った `waypoints` は指示と一緒に次ラウンドへそのまま引き継ぐ（既存の「指示維持」と同じ）
- 既存の「goto到達→guard化→次ラウンドでrushに上書き」ロジック（`advanceToNextRound()`）は、判定条件を `targetHex` 参照から `waypoints` 参照に書き換えて維持する（guard化かつwaypointsが残存＝自動切り替え由来、の判定。手動guardは従来通り`setUnitPurpose()`でwaypointsをnullにするため区別できる）

### A-4. 「敵拠点へ」＋経由点（仕様拡張・要UC最終確認）

- ドラッグの終点が敵拠点だった場合（B-2参照）、`purpose = "rush"` かつ `waypoints` に経由点のみ（最終目的地は含めない）を設定できるようにする
- シミュレータ側: rushユニットに `waypoints` があれば、A-2と同じ手順で経由点を先に消化し、**全経由点消化後に従来のrush（敵拠点へ最短）に戻る**
- 経由点のないrushは従来と完全に同一挙動（回帰させないこと)

### A-5. 検証（ブラウザコンソールで`simulateBattle()`直接呼び）

- 経由点2個のgoto: 順番通りに曲がって進み、各経由点は隣接で消化されること
- 経由点をキャラで塞ぐ → そのtickは次の目標へ向かい、封鎖が解けたら復帰すること
- 全経由点＋最終目的地が到達不能 → 停止（行動力を浪費しない）こと
- 最終目的地到達 → guard化、次ラウンドでrush化（既存挙動の回帰確認）
- 経由点付きrush: 経由点を辿ってから拠点へ向かうこと。経由点なしrushが従来と同一であること

---

## B. ドラッグ指示UI

### B-1. 操作体系（統一後の全体像）

| 操作 | 意味 |
|---|---|
| 自分のユニットを**タップ** | 選択（指示パネル表示）。従来通り |
| 自分のユニットから**ドラッグ開始→線を引く→離す** | 離した位置に応じて指示を設定（B-2） |
| ユニット以外から**ドラッグ** | 従来通り（何もしない/ブラウザ既定）。指示操作はユニット起点のみ |

- **既存の「ドラッグで指示コピー」（`startDragPaint`/`applyDragPaint`、通過した味方に指示を塗る機能）は廃止**し、本方式に一本化する。関連コード・イベントリスナー・`dragPaintMoved`フラグ周りを削除
- 対象は**自分の・そのラウンドで指示変更可能なユニット全て**（前ラウンド引き継ぎユニットも指示変更は自由なので対象。位置変更不可とは別の話）
- 指示パネルのチップUI（4種）は**残す**。ドラッグと併存し、どちらでも設定できる（「守る」やピンポイント修正はパネルの方が確実な場面がある）

### B-2. 終点の解釈

ドラッグを離した位置で分岐:

| 終点 | 設定される指示 |
|---|---|
| **空きマス**（通行可能マス） | 指定マスへ向かう（`goto`）。経由点＝線の曲がり角、最終目的地＝終点マス |
| **敵拠点**（拠点マスまたはその周辺1マス以内） | 敵拠点へ最短（`rush`）。線に曲がり角があれば経由点として設定（A-4） |
| **敵ユニット** | 敵キャラを追う（`chase`）。経由点は**採用しない**（chaseは動的ターゲットのため線の形は無視） |
| **味方ユニット**（起点以外） | **起点ユニットの現在の指示を終点ユニットにコピー**（旧ドラッグコピーの代替。gotoの場合はwaypointsごとコピー） |
| **起点ユニット自身のマス**（ドラッグしたが自分の上で離した） | この場を守る（`guard`）。waypointsはnull |
| 壁マス・盤外 | キャンセル（指示変更なし） |

- タップ（ドラッグ距離がしきい値未満、例: 8px）は選択として扱い、指示は変更しない
- goto設定時、既存の「チップからgotoを選ぶ→マス選択モード」フロー（`mode="setTarget"`）は残してよいが、その場合のwaypointsは終点1個のみ（経由点なし）

### B-3. 曲がり角の自動抽出

- ドラッグ中、ポインタが通過したヘックスの列を記録する（同一ヘックス内の移動は無視、重複は直近と同じなら追加しない）
- 隣接ヘックス間の移動方向（6方向）を追跡し、**方向が変わったヘックスを経由点として抽出**する。直線区間の中間ヘックスは捨てる
- ポインタが飛んだ場合（速いドラッグでヘックスが連続しない）は、直前ヘックスとの間を補間せず、そのまま2点間の方向変化として扱ってよい（厳密さより実装の単純さを優先）
- 折り返し（同じマスを2度通る等）もそのまま曲がり角として採用してよい。異常に長い線への防衛として経由点の上限を**10個**とし、超えた分は**間隔が均等になるよう間引く**（最終目的地は必ず保持）

### B-4. モバイル対応・競合防止

- ユニット起点の`pointerdown`後は`preventDefault`＋`touch-action: none`相当の制御でページスクロール・ブラウザのタッチ既定動作を抑止する
- 盤面ズーム機能（`boardZoom`、svgのwidth/height変更方式）とは干渉しないはず（viewBox固定なので座標変換は既存クリック判定と同じ）だが、ズーム60%と260%の両端で座標ずれがないこと確認
- `pointercancel`（着信等での中断）はキャンセル扱い（指示変更なし）

### B-5. 検証

- マウス・タッチ両方で: 各終点6パターン（空きマス/敵拠点/敵ユニット/味方ユニット/自分自身/壁）の分岐が正しいこと
- タップが従来通り選択になること（誤って守るにならないこと）
- 曲がった線→waypointsが曲がり角のみになっていること（直線中間マスが入っていないこと）
- 旧ドラッグコピー廃止後、通常クリック・配置・移動等に回帰がないこと（過去に`dragPaintMoved`で盤面クリック全無視の回帰バグを踏んでいる。CLAUDE.mdの該当記録を読んでから触ること）
- オンライン2タブ: waypoints付きユニットがFirebase経由で正しく同期・復元されること（リロード復帰含む）。相手には指示・線・waypointsが一切見えないこと（既存の秘匿仕様の維持）

---

## C. 線の表示3層

### C-1. 表示の基本方針（3層）

1. **通常時**: 線は表示しない。既存の指示バッジ（マーカー右下角の円形バッジ）のみ
2. **選択時**: 選択中の1体だけ、指示の線をフル表示（C-3の確定線）
3. **全軍俯瞰**: 専用ボタンを**押している間だけ**、自分の全ユニットの確定線を同時表示（`pointerdown`で表示、`pointerup`/`pointerleave`で消す）。配置画面の盤面近くに配置

### C-2. ドラッグ中のプレビュー線（行動力ゲージ兼用）

ドラッグ中、起点からポインタ現在位置まで、通過ヘックス列に沿った線をリアルタイム描画する。この線は**行動力の見積もり表示**を兼ねる:

- 線の太さは起点で最大とし、**そのユニットの現在の行動力**（基礎値＋繰り越し分`apCarry`。配置画面の「行動20+10」表示と同じ値）に比例させる
- 1ヘックス進むごとに太さを1段階減らす。**移動ボーナスN**持ちはNマスごとに1段階（1消費でNマス進めるため）
- 行動力が尽きる地点で最細になり、**それ以降は点線**で描く（「今ラウンドはここまで、点線区間は次ラウンド以降」の表現）
- 罠マスの追加消費は計算に**含めない**（初期版の割り切り。将来検討）
- R4のHP→行動力変換も**含めない**（見積もりは通常ルールのみ）
- 線の終端（ポインタ位置）に**その地点到達時の残り行動力の数字**を小さく表示する。**この数字は仮採用**——見た目がうるさければ外すとUCが判断するため、定数フラグ1つ（例: `SHOW_AP_PREVIEW_NUMBER`）でON/OFFできる作りにしておくこと

### C-3. 確定線（選択時・俯瞰時に出す線）

- waypointsを順に直線（ヘックス中心を結ぶポリライン）で結び、最終目的地に既存の×印/マークを表示する。実際の戦闘経路（BFS結果）を描く必要はない——線は「指示の写像」であり経路保証ではない
- 太さ・点線の表現はC-2と同じ計算を適用（起点=ユニット現在地から再計算）
- rush（経由点なし）の確定線は、ユニット→最寄りの敵拠点への直線1本で表現。chaseは線を出さずバッジのみ（対象が動的なため）。guardは線なし
- 色は陣営色ベース。指示の種類はバッジ側が既に担っているので、線での色分けは必須ではない（見やすさ優先で調整可）

### C-4. 既存表示との整合

- 既存の「goto目的地への点線＋×印」常時表示は、本実装の3層方針に置き換える（常時→選択時/俯瞰時のみ）
- 指示バッジ・向きウェッジ・HPバー・罠マークは現状維持
- 相手ユニットの線・バッジ非表示（秘匿）は引き続き厳守。`isOwn`判定の対象に線描画も含めること
- 戦闘画面では線を表示しない（配置フェイズ専用）

### C-5. 検証

- 行動力15のユニットで15マス超の線を引く→尽きる地点から点線になること。移動ボーナス持ち（該当キャラがいなければテスト用に一時付与）で減衰が緩いこと
- apCarry持ちユニット（前ラウンド温存）の線が新品ユニットより太い/長いこと
- 選択・俯瞰・通常の3状態の切り替えに描画残りがないこと
- ズーム両端・モバイル実機での視認性

---

## D. 完了後のドキュメント反映

- 設計書§4に「経由点」「ドラッグ指示」の節を追加（本書A-2/A-4/B-2の内容）。§10の未確定リストに「罠マスをプレビュー計算に含めるか」「残り行動力数字の採否」を追記
- CLAUDE.mdの実装済み機能・コア仕様要約を更新（targetHex→waypointsのデータ変更、ドラッグコピー廃止を明記）
- ルール説明ポップアップ（`#rulesScrim`）の指示の節に、ドラッグ操作の説明を追記
