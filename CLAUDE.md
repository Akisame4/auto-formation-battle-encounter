# CLAUDE.md — AFBE（Formation Battle Encounter）

このファイルはClaude Codeがセッション開始時に必ず読むこと。コンテキスト圧縮や長時間セッションで仕様がぼやけたら、本ファイルと `AFBE_システム設計書.md` を再読すること。

## プロジェクト概要

2人対戦オンライン・オートバトラー。ヘックスマップにキャラを配置し指示を出し、戦闘はtick制で自動解決。全4ラウンドの消耗戦（HP・位置・罠は引き継ぎ、行動力のみ回復）。

## 絶対原則

1. **仕様の正典は `AFBE_システム設計書.md`**。矛盾する実装をしない。仕様が曖昧・未記載の場合は**推測せず必ずUCに確認**する
2. **単一HTMLファイル**構成。フロントは1つの `index.html` に完結（CSS/JS込み）。外部依存はFirebase SDKのみ
3. **Firebase Realtime Database**で同期。戦闘結果は同期しない — **共有シードによる決定論的シミュレーション**で両クライアントが同一結果を計算する
4. **ロジックと演出の完全分離**。戦闘シミュレータは純粋関数的に「イベントログ」を生成し、演出層はログを再生するだけ。シミュレータはUIなしで単体テスト可能にする
5. 乱数はすべて共有シードから生成する（Math.random()直接使用禁止。シード付きPRNGを1本用意し全乱数をそこから引く）

## 実装済み機能

- **P1: 盤面基盤**（`index.html`）
  - 座標系: オフセット座標 (col 0-19, row 0-18) を基本表現とし、pointy-top・odd-r方式でヘックス敷き詰め。距離計算はaxial座標 (q=col-floor(row/2), r=row) に変換して算出
  - row 0-6 = プレイヤー1の自分側半分 / row 7-11 = 中立地帯（拠点・自陣とも不可、両陣営の隣接を防ぐ帯）/ row 12-18 = プレイヤー2の自分側半分
  - **[2026-08-23] マップ縮小**: UCから「マップが大きく見づらい」との指摘を受け、旧30行からrow7-9・13-17・20-22（計11行）を削除し残り19行を0〜18に振り直した。列見出しもUI上は数字ではなくアルファベット表記（`colLabel()`、col0=A〜col19=T）。壁・拠点レイアウトも同時に再設計（下記）
  - 壁マスは`WALLS_HALF`定数で定義（180度点対称でミラー生成）。P1/P2側はrow5の5レーン・チョークポイント等、中立地帯(row7-11)はcol6・col13の縦仕切りで左/中央/右の3レーンに分割（row9のみ通行可）＋左右レーン内の岩場
  - **[仕様変更 2026-08-16] 拠点は固定位置**。`FIXED_BASES`定数（P1: (5,0)/(14,0)、P2: (5,18)/(14,18)。180度点対称、現行`WALLS_HALF`と非衝突）でR1開始時・全リセット時に自動配置。手動設置UIは廃止（旧: プレイヤー切替→自陣内クリックで2個まで設置、というUIがあったが削除）
  - 自陣判定: 各拠点からヘックス距離3以内（壁マス除く）を自動ハイライト

- **P2: 配置フェイズ（ローカル）**（`index.html`、R1のみ対応。R2以降のクレジット・繰越・プール拡張・強化はP6/P7で扱う）
  - `CHARACTERS`定数に設計書§9の初期プール8体を定義。購入UIはプレイヤーごとの残クレジット（R1固定200）を見て購入可否を判定
  - 購入したキャラは「配置待ちキュー」に入り、キューから選択→自陣内クリックで盤面に配置（`computeTerritory`を再利用。壁・自陣外・重複を弾く）
  - 盤面上の自分のキャラをクリックすると選択状態になり、指示（目的4種: rush/guard/chase/goto）をラジオボタンで設定可能。goto選択時は「目標マスを指定」ボタン→盤面クリックで目標マスを設定（盤面上に点線と×印で表示）。既存キャラの指示はいつでも変更可能
  - 射撃N系スキル（`needsFacing()`で判定）を持つキャラのみ、6方向ボタンで向きを指定・変更でき、盤面上に向きを示す小さいマーカーを表示
  - モード管理: `mode`（idle/placeUnit/setTarget）で盤面クリックの意味を切り替え。bases未設置時のクリックは拠点設置、拠点2個設置後は無視（キャラ選択/配置/目標指定が優先）
  - 「全てリセット」ボタンで拠点・キャラ・キュー・クレジットを全て初期化するよう拡張済み

- **P3: 戦闘シミュレータ**（`index.html`内、DOM非依存の純粋関数群。`simulateBattle(unitsInput, basesInput, seed, options)`が唯一のエントリポイント）
  - シード付きPRNG（mulberry32）・BFS経路探索・hexライン射線判定・tickループ（§5-1〜5-2）・移動/攻撃AI（目的4種、「守る」の行動順位制御込み。**2026-09-05仕様変更、詳細は本ファイル該当セクション参照**）・行動力繰越（半分切り上げ・基本値2倍上限）・スキル17種すべてを実装し、ブラウザコンソールから`simulateBattle()`を直接呼ぶ形で一通り動作検証済み
  - 出力はイベントログ（tick付き: move/attack/damage/death/baseDestroyed/poison/push/pull/linkedAttack/heal/followMove/musouRecover/purposeChange等。**2026-09-05に`counter`種別は廃止**）+ 最終状態（各ユニットの`carryoverAp`込み）
  - **スコープの意図的な線引き**: tickループの終了条件は§5-1の機械的なもの（全キャラ行動力0、または全員停止状態＝手詰まりで終了。**2026-09-05**: 手詰まり検出を厳密化し50tick上限の安全装置は廃止）のみ実装。§6-1の即時終了判定（全滅・拠点陥落・同時陥落の特殊ルール）とラウンド勝敗（§6-2）はP6の仕事として、ログ+最終状態から解釈する設計とした
  - **暫定解釈・簡略化した点**（コード先頭のコメントにも記載。将来の見直し候補）:
    - 押し込み/引き寄せの方向は「攻撃者→対象ベクトルに最も近い6方向」で決定（**2026-08-19確定**。設計書§8「方向裁定」参照。タイは配列順で先着優先）
    - 拠点は移動の障害物として扱う
    - 範囲攻撃で複数ヒット時、押し込み/引き寄せは対象ごとに独立して50%判定
    - 追従Nは「移動のたび即座にトリガー判定」する簡易実装。同一tick内で複数候補が競合する場合の厳密な優先順位付け（§8の「複数候補」規定）は簡略化

- **経由点システム（Block A、2026-09-15実装）**（`index.html`）。UCが別セッションで作成した実装指示書 `指示書_ドラッグ指示と経由点.md` のBlock A部分。ドラッグ指示UI（Block B）・線の表示3層（Block C）はまだ未着手
  - **[2026-09-15訂正]** 実装後にUCへ確認したところ、指示書の前提と食い違いが判明。①指示パネルのチップUI（4種ボタン）は完全廃止しドラッグのみに一本化する、②「rush」（敵拠点への自動追尾）という仕組み自体が不要（拠点近くへの矢印は`goto`として扱い、到達後の「守る」化＝射程内自動攻撃だけで十分）、の2点。下記のA-4（rush+経由点）はこの訂正により丸ごと不要と判明したが、コード上の削除・チップUI全廃はBlock B実装時にまとめて行う方針（UC了承済み）。それまでは`rush`・A-4対応が暫定的にコードに残る
  - ユニットの`targetHex`（単一マス）を`waypoints`（マスの配列。`purpose==="goto"`の時は最低1要素・配列の最終要素が最終目的地）に置き換えた。**後方互換**: `initBattleState()`で旧形式`targetHex`しか無いデータを`waypoints=[targetHex]`に自動変換するため、移行前のFirebaseデータや保存済み状態でも壊れない
  - `consumeWaypoints(unit)`: `waypoints[0]`が現在地と同一マスまたは隣接（`hexDistance<=1`）なら消化（配列から除去）を繰り返す。`purpose==="goto"`の時だけ最終要素を保護（隣接では消化しない。実マス到達のみ、`performAction`側で判定）。rushの経由点（後述）には保護なし
  - `pickReachableWaypoint(state, unit)`: 残ったwaypointsを先頭から見て、`findPath()`で経路が引ける最初の要素を返す。一時的に塞がれた経由点はそのtickだけ飛ばし、配列からは消さない（次tickで封鎖が解ければ元に戻る）。全滅（`findPath`が全要素`null`）ならそのtickは停止（行動力を浪費しない）
  - `computeGoal()`のgoto分岐はこの2関数を使うよう書き換え。rush分岐にも同じ仕組みを追加し、`waypoints`が設定されていれば先に経由点を辿ってから（全消化後）従来の「最寄りの敵拠点」に戻る（UC指示書A-4。経由点なしrushは完全に既存動作のまま）
  - `advanceToNextRound()`の「goto到達→guard化→次ラウンドでrush上書き」判定は`targetHex`参照から`waypoints`参照に書き換え（意味は変わらず、単純な項目名の追従）
  - 既存UIの単発クリック指定（購入→チップで「指定マスへ」→盤面クリック）はそのまま残し、内部的に`waypoints=[{col,row}]`という要素1個の配列を生成する形に統一した（Block Bのドラッグ入力が実装されるまでの暫定動作）
  - **踏んだバグ**: `pickReachableWaypoint`が`findPath()`の戻り値を`path && path.length > 0`で判定していたが、`findPath()`はstart===goal（＝既にその経由点にいる）の場合に`[]`（空配列・truthyだがlength0）を返す仕様だったため、「既にそこにいる」を誤って「到達不能」と判定してしまう潜在バグがあった。手動テストの単純な経路では顕在化しなかったが、まだ消化されていない別の経由点がたまたま現在地と一致する構成（ジグザグな経路等）で発生しうるため、`if (path)`（null判定のみ）に修正。**教訓: BFS系関数の戻り値を「truthyか」だけで判定せず、`[]`（0要素だが成功）と`null`（失敗）を区別する既存の意味論を必ず確認すること**
  - 検証: ブラウザコンソールから`simulateBattle()`を直接呼び、指示書A-5の全項目（2経由点の順次消化・隣接での消化・一時封鎖からの復帰・全封鎖時の停止＝行動力温存・rush+経由点→通常rushへの復帰・経由点なしrushとの回帰一致・後方互換targetHex変換）を確認。加えて実機UI（購入→チップでgoto設定→戦闘→次ラウンド）でも「目標到達→guard→次ラウンドでrush」の一連の流れをコンソールエラーなしで確認済み

- **ドラッグ指示UI（Block B、2026-09-15実装）**（`index.html`）。Block A完了後の訂正（上記参照）を反映し、指示パネルのチップUI（4種ボタン）を完全廃止してドラッグのみに一本化。あわせて「rush」（敵拠点への自動追尾）をコードから完全に削除した。線の表示3層（Block C、確定線・ドラッグ中プレビュー）はまだ未着手
  - **rush撤去**: `computeGoal()`のrush分岐（Block AのA-4対応込み）を削除。`consumeWaypoints()`は常に最終要素を保護する形に単純化（呼び出し元がgoto専用になったため）。`performAction()`の到達判定2箇所で`purpose="guard"`にする際`waypoints`も一緒にnull化するようにし、`advanceToNextRound()`の「goto到達→次ラウンドでrush上書き」特殊分岐は完全に削除（到達済みユニットは自然に`waypoints:null`のguardとして素通しされる）。`handlePlaceUnit()`の新規配置デフォルトは`rush`→`guard`に戻した。`PURPOSES`配列は削除し`PURPOSE_LABEL`（読み取り専用表示用）に置き換え、`PURPOSE_SHORT`・マーカーバッジ用マップから`rush`エントリを除去
  - **チップUI廃止**: `renderUnitEditPanel()`の`.purpose-chip`生成ループを、`現在の指示: ${PURPOSE_LABEL[...]}`という読み取り専用の1行＋「キャラからドラッグして指示を変更できます」という案内文に置き換え。`setUnitPurpose()`・`handleSetTarget()`関数と`mode==="setTarget"`分岐を削除（チップ起点の入力経路が丸ごと不要になったため）。CSSの`.purpose-row`/`.purpose-chip`も削除
  - **新ドラッグ指示システム**: 旧・一括指令ドラッグ（`startDragPaint`/`applyDragPaint`、`mousedown`/`mouseenter`ベース）を削除し、Pointer Events（`pointerdown`/`pointermove`/`pointerup`/`pointercancel`）ベースの新方式に置き換えた。タッチはpointerdownした要素に暗黙キャプチャされ他要素の`pointerenter`が発火しないため、`pointermove`のたび`document.elementsFromPoint()`で「今どのヘックスの上か」を解決する方式にした（ボードズームにも自動追従）。しきい値8px未満の移動はタップ扱いとし、既存の選択動作を壊さない（旧`dragPaintMoved`と同じ「pointerup直後に同期発火するclickを1回だけ無視し、次イベントループでリセットする」バグ回避パターンを`unitDragMoved`として踏襲）
  - **ドラッグの終点解釈**（指示書B-2、rush撤去に伴い統合済み）: 空きマス・拠点マス問わず→`goto`（経由点は`extractWaypoints()`で曲がり角のみ抽出、上限10個・超過分は間引き）／自分自身に戻す→`guard`／敵ユニット→`chase`／自分の別ユニット→指示をコピー（gotoなら`waypoints`ごと）／壁マス・盤外→キャンセル
  - **踏んだバグ**: 実装直後の実機ドラッグテストで、2点目以降のドラッグが何も起きず失敗する不具合が発生。原因はBlock Aで追加したgoto目標線（`<line>`要素、ユニット位置から目標マスへの点線）に`pointer-events:none`が付いておらず、この線がユニット自身のマスのピクセル座標（線の`x1,y1`）にちょうど重なって`elementFromPoint()`の結果を奪っていたため、2回目以降のドラッグ起点となる`pointerdown`がヘックスタイルではなく線要素に飛んでしまっていた（1回目のドラッグでgoto線が描画されるまでは問題が起きず、テストの後半で初めて顕在化した）。**教訓: SVGオーバーレイ要素を追加する際は`pointer-events:none`の付け忘れがないか必ず確認する**（このプロジェクトでは拠点・ユニットマーカー等に同様の対応が既に多数あり、既存パターンとして徹底されていたはずだったが、Block Aで新規追加した目標線1点だけ漏れていた）。再発防止として`resolveHexAtPoint()`は`elementFromPoint()`ではなく`elementsFromPoint()`（重なっている全要素を返す）を使い、個々のオーバーレイ要素のpointer-events設定に依存せず`.hex-clickable`を確実に見つけられるよう防御的に実装し直した（Block Cで線描画が増える前提の保険）
  - 検証: Chrome拡張から`PointerEvent`を直接dispatchして実際のドラッグ操作を再現し、B-2の全終点パターン（空きマス→goto・経由点1個の曲がったパスも含む／自分自身→guard／敵ユニット→chase／自分の別ユニット→指示コピー／壁マス盤外→キャンセル／しきい値未満→タップ扱いで指示変更なし）を実機で確認。戦闘実行→goto到達→次ラウンドで`guard`のまま（`rush`に変わらない）ことも確認。コンソールエラーなし。**タッチ実機（スマホ）での確認は今回のセッションでは自動化できておらず未実施**——Pointer Events自体はタッチ・マウス両対応の設計だが、実機でのタップ/ドラッグ操作感の確認はUC自身に依頼する必要がある

- **線の表示3層（Block C、2026-09-16実装）**（`index.html`）。Block Bで実装したgoto常時表示線を、指示書C-1の3層方針（通常時はバッジのみ／選択時に確定線／全軍俯瞰ボタン押下時のみ全ユニットの確定線）に置き換えた
  - **共通ジオメトリ**: `expandChainToHexes(chain)` — 経由点（疎な曲がり角のみの列）を、既存の`hexLineCells()`（射線判定と同じ幾何補間、壁・占有を無視した純粋なピクセル計算）で隣接ヘックス単位の密な列に展開する。「指示の写像であって戦闘経路と一致しなくてよい」という指示書C-3の方針通り、BFS結果は使わない
  - **共通描画**: `drawApScaledPolyline(group, chain, unit, char, color, showEndNumber)` — 展開後のヘックス列を1ヘックスごとの`<line>`に分割し、行動力（`char.ap + apCarry`）に応じて太さを線形に減衰させ、移動ボーナスN持ちはNマスごとに1段階減（1消費でNマス進めるため）。行動力が尽きた区間（`apAtStart<=0`）は最細＋`stroke-dasharray`で点線化。終端に×印、`showEndNumber`時（ドラッグ中プレビューのみ）は残り行動力の数字も表示する。指示書C-2で「仮採用」と明記された数字表示は`SHOW_AP_PREVIEW_NUMBER`定数でON/OFF可能にした
  - **確定線（C-3）**: `render()`の per-unit ループで、`isOwn && purpose==="goto" && (isSelected || overviewMode)`の時だけ`drawApScaledPolyline(gMarkers, [unit位置, ...waypoints], ...)`を呼ぶ（Block Bまでは常時表示だった簡易版をこれに置き換え）。chase/guardは元々線を出さずバッジのみ（rush撤去によりC-3のrush専用記述は不要になった）
  - **全軍俯瞰（C-1の3層目）**: `#overviewLinesBtn`（配置画面の盤面ズームコントロール内、`.board-overview-btn`）を`pointerdown`で押している間`overviewMode=true`にして`render()`、`pointerup`/`pointerleave`/`pointercancel`で`false`に戻す。押している間だけ自分の全goto線が見える
  - **[2026-09-16改修] UC要望「他のキャラをクリックしたら線が消えるのが気になる、薄っすら残ってほしい」**への対応。全軍俯瞰を「押している間だけ表示」の一時トグルから、クリックで切り替わる**ON/OFFトグル**（`#overviewLinesBtn`、`click`一発で`setOverviewMode(!overviewMode)`、ボタンに`.on`クラス＋ラベル文言"ON"/"OFF"で状態を表示）に変更。OFF時は従来通り選択中の1体だけフル表示、**ON時は選択中の1体はフル表示のまま、それ以外の自分の全goto持ちユニットの線も薄く（`ORDER_LINE_FAINT_OPACITY=0.32`倍）常時表示**する。これにより選択を切り替えても前のキャラの線が完全に消えず薄く残って見える。`drawApScaledPolyline()`に`opacityScale`引数を追加し、線・×印の不透明度に掛けて共通化した（太さ計算はフル/薄いずれも同じ）
  - **ドラッグ中プレビュー（C-2）**: 新設のSVGグループ`gDragPreview`（`gMarkers`とは別の独立グループ。`render()`の`gMarkers.innerHTML=""`で消えないようにするため）を使い、Block Bの`pointermove`ハンドラから`unitDragPath`が更新されるたびに`renderDragPreview()`を呼んでリアルタイム再描画する（`render()`本体は呼ばない＝軽量）。`pointerup`/`pointercancel`で`gDragPreview.innerHTML=""`にして消す。`enterBattleMode()`/`exitBattleMode()`で`gMarkers`と同様に表示/非表示を切り替え、戦闘画面では出ない
  - ドラッグで指示を確定した瞬間（`applyDragInstruction()`）に`selectedUnitId`をドラッグ元ユニットに設定するようにした。これにより指示を出した直後、追加の操作なしにその場で確定線が見える
  - 検証: ブラウザ操作の途中でChromeのウィンドウが0×0として認識される環境不具合が発生し、一時的にスクリーンショット・座標ベースの操作が不可能になった（新規タブ・新規タブグループでも再現）。UCに依頼してChromeの状態を確認してもらった後、改めて新規タブを作成したところ復旧した。**原因不明・このプロジェクトのコードとは無関係な環境要因**とみられる。この間、行動力に応じた太さ/点線の計算式（`drawApScaledPolyline`と同じ数式）と経由点展開（`expandChainToHexes`）をNode.js上でDOM非依存の純粋関数として切り出し、単体テストで検証（行動力15で15ヘックス目から点線化・移動ボーナス2で消費ペースが半分になることを確認）。復旧後、実機（Chrome拡張、PointerEvent直接dispatch）でドラッグ中プレビュー（Node検証と一致する太さ/点線の進行・終端の残り行動力表示）→確定線（選択時のみ表示・非選択で消える）→全軍俯瞰ボタン（押下中のみ全表示）→戦闘画面（`gMarkers`/`gDragPreview`とも非表示）→戦闘後の配置画面復帰（両グループとも表示復帰）まで一通り確認。コンソールエラーなし。移動ボーナス持ちキャラが現在のロースター（8体）に存在しないため、その減衰パターンの実機確認はNode検証のみ

- **P4: 戦闘演出**（`index.html`、「戦闘」パネル）
  - 「戦闘開始」ボタンでP2の盤面状態（`bases`/`units`）から`simulateBattle()`を呼び、返ってきたログを`eventsByTick`に整形してtick単位で再生する。**演出層はログを適用するだけでロジックを再計算しない**（初期状態の再構築も`initBattleState`をそのまま再利用）
  - 移動/押し込み/引き寄せ/追従移動はマーカーをtweenで滑らかに追従、攻撃はダメージ数字（赤字/毒緑）と攻撃者→対象への一瞬の線エフェクト、撃破はフェードアウト、毒は紫ドット表示、HPバーは全キャラ・拠点共通で表示
  - 再生コントロール: 一時停止付き自動再生（tickごと）、1x/2x/4x速度切替、「最後まで」で`battleResult`の最終状態を直接反映（アニメ省略・ログ欄は一括表示）、「最初」で再生前状態に巻き戻し
  - ログ欄に人間が読める形式（`[t5] 攻撃 ソルジャー → ガーディアン`等）で全イベントを表示
  - 戦闘中は配置フェイズの盤面クリック（`onHexClick`）を無効化。「全てリセット」で戦闘状態も含めて初期化される
  - スキップ時の最終状態が`simulateBattle()`の戻り値と完全一致することをブラウザ実機で確認済み（ログ再生がロジックの結果と乖離しないことの検証）

- **P5: Firebase同期**（`index.html`、ロビーパネル。メインスクリプトは`type="module"`化しFirebase JS SDK v10をCDNから直接importする）
  - **ルームコード方式**（合言葉）でマッチング。ホストが4桁数字を発行→ゲストが入力して参加。自動マッチングは不採用（UC確認済み）
  - Realtime Databaseのデータ構造（`firebaseConfig`はスクリプト冒頭にコメント付きで記載。projectId=`auto-batoen`）:
    ```
    /rooms/{roomCode}/
      status       : "waiting"（ホストのみ在室） | "placing"（両者在室） | "battle"（両者ready・開戦）
      createdAt    : サーバータイムスタンプ
      seed         : 両者readyになった時点でホスト(player1)が発行する共有乱数シード
      hostOnline   : bool（onDisconnectで自動false化）
      guestOnline  : bool（onDisconnectで自動false化）
      placements/
        player1/ { bases:[...], units:[...], queue:[...], credits, ready:bool, pool:[...] }
        player2/ { bases:[...], units:[...], queue:[...], credits, ready:bool, pool:[...] }
    ```
  - **[2026-09-14] デッキ編成機能を追加**。詳細は下記「デッキ編成」の項と設計書§9-1を参照。R1のプール抽選が「初期プール8体から共有シードで6体」→「自分のアクティブデッキから6体を各自ローカルに抽選」に変更されたため、上記スキーマから`poolSeed`/`pool`（room直下）は廃止し、代わりに各プレイヤーの抽選結果を`placements/{role}/pool`に持たせて再接続時に復元する形にした
  - **秘匿はUI制御のみ**（相手のplacementsを配置フェイズ中はローカル状態に取り込まない・描画しない）。開発者ツールで覗けば技術的には見える制約はUC了承済み（サーバーサイドロジックなしの単一HTMLファイル構成のため）
  - **戦闘結果は同期しない**。両者readyでseed・placementsが揃った時点で各クライアントが`simulateBattle()`をローカルで独立実行し、共有シードにより同一結果になることを担保（P3の設計をそのまま流用、無修正）
  - 自分側の配置状態（`bases[myRole]`/`units.filter(owner===myRole)`/`queue[myRole]`/`credits[myRole]`）は`render()`末尾から`pushMyPlacement()`が400msデバウンスで自動プッシュ。「配置完了」ボタンで`ready:true`を即時反映
  - **切断・リロード復帰**: `localStorage`に`{roomCode, role}`を保存し、ページ読み込み時に`tryAutoRejoin()`が同じ役割で再接続し、Firebase上の自分のplacementsをローカルへ復元する（UC確認済み・同じ合言葉での復帰を想定）
  - ルーム未参加時は既存のデバッグ用P1/P2手動切替UIがそのまま使えるオフラインテストモードとして残置（当初案の「P5導入時に削除」からUC相談なしで方針変更。理由: ローカル単体テストの利便性を優先）

- **デッキ編成**（`index.html`、2026-09-14実装。UC要望「将来的に30体くらいまでキャラを追加して、対戦前にデッキ作成をできるようにしたい」への対応）
  - ページ読み込み時、ロビー画面より前に`#deckScreen`を表示。3枠まで保存できるデッキから1つをアクティブに選び「対戦へ進む」でロビーへ（`initApp()`が`loadDecks()`→保存済みルームへの復帰 or デッキ画面表示、を振り分け）
  - `DECK_SIZE = Math.min(12, CHARACTERS.length)`（現状8体のため実質「全部選ぶ」形だが、将来30体になった時に12体まで選べる設計）。`DECK_SLOT_COUNT = 3`
  - 保存先はFirebase `/decks/{deviceId}/{activeSlot, slots/{0,1,2}/{name,characterIds,updatedAt}}`。ログイン機能が無いため`localStorage`の`afbe_device_v1`キーに`crypto.randomUUID()`を永続化し擬似アカウントIDとして使う（ルーム再接続用の`afbe_room_v1`は`sessionStorage`＝タブ単位・意図的に別物。混同注意）
  - 初回アクセス時（`decks/{deviceId}`が未存在）は`loadDecks()`が自動でデッキ1（全キャラ）を作成し、デッキ編集を強制しない
  - **R1プールの仕組みを変更**: 旧方式（ホストが`poolSeed`をFirebaseに書き込み両者が同一の6体を見る）を廃止し、`regenerateRound1Pool(seed)`が`myDeckCharacterIds`（アクティブデッキの構成）から`pickRandomPool()`で6体を各クライアントが独立に抽選する方式に変更。相手の新規購入は元々非公開情報なので同期不要（`pickRandomPool(seed, count, idPool)`は第3引数でid列を受け取れるよう汎用化、省略時は旧来通り全キャラ）
  - 抽選し直しが必要なタイミング（アクティブデッキが変わりうる箇所すべてで呼ぶ必要がある）: `createRoom()`/`joinRoom()`（enterRoom前）、`devSkipLobbyBtn`クリック時（オンライン経路を通らないため個別に必要）、`resetGameState()`（既存のR1リセットと連動）
  - **再接続時にプールを再抽選しない**よう`myPlacementSnapshot()`/`restoreMyPlacement()`に`pool`フィールドを追加。`tryAutoRejoin()`は`placements/{role}`が存在すれば`pool`ごと復元し、存在しない（＝一度も購入していない）場合のみ新規抽選する
  - **ハマったバグ**: `initApp()`内で`loadDecks()`直後に1回だけ`regenerateRound1Pool()`を呼んでいたが、その後ユーザーがデッキ画面でアクティブデッキを切り替えても（`selectActiveSlot()`は`myDeckCharacterIds`は更新するが`currentPool`の再抽選まではしない）、この最初の抽選結果が上書きされずに残ってしまい、オフラインテスト（dev-skip経由）で「切り替えたはずのデッキと違うプールが出る」不具合になった。原因は「アクティブデッキが変わりうる箇所すべてでプールを再抽選する」というルールを`devSkipLobbyBtn`の1箇所だけ入れ忘れていたこと。**教訓: 同種の呼び出しが必要な箇所を洗い出す時は、`createRoom`/`joinRoom`のような「本流」経路だけでなくdevスキップのような迂回経路も見落とさないこと**（`[2026-08-22]`のHP→行動力変換バグと同種の「複数箇所への同じ対応漏れ」パターン）
  - 実機検証（Chrome拡張、`python -m http.server`経由）: デッキ画面の自動作成・編集（キャラ選択トグル・上限12・名前変更）・保存・アクティブ切替・永続化（リロード後も維持）を確認。2タブでのオンライン対戦で両プレイヤーのR1ショップに出るキャラが独立して異なることを確認（旧: 完全一致→新: 基本的に一致しない）。購入後にリロードしても同じプール・同じ購入内容が復元され再抽選されないことを確認。コンソールエラーなし

- **P7: 4R特殊処理**（`index.html`、2026-08-22実装）
  - **HP→行動力変換**（§6-3）: `convertHpForAp(state, unit, apNeeded, log, tick)`。`state.round4`が真のときのみ有効（`simulateBattle()`に`{round4: currentRound===4}`を渡す）。行動力が必要数に届くまでHP10（端数は残り全部）を消費して+1ずつ変換し続け、HPが0になったら死亡してその行動は不発（UC確認済み: 1行動に必要な分だけ何度でも変換、端数HPも全部使って0→死亡）
  - 適用箇所: `performAction`の攻撃・移動コスト確定直後。tickの行動可能者フィルタも`ap>0 || (round4 && hp>0)`に緩和（**2026-09-05**: `checkCounter`は「守る」の仕様変更で廃止されたため、適用箇所は`performAction`のみに統一された）
  - **実装時に踏んだバグ**: tickループには行動可能者を決める`actorIds`フィルタ（外側）と、実際に`performAction`を呼ぶ直前の重複フィルタ（内側、`for (const id of order)`内）の2箇所に`ap<=0`のガードがあった。外側だけR4対応して内側を直し忘れたため、R4でもap=0になった時点でperformActionが一切呼ばれず、tickだけ50まで無為に消化される（HPも減らず変換も起きない）静かなバグが発生した。実機テスト（ブラウザで`window.__debug*`に一時的に戦闘結果を露出させて`log.length`やイベント種別集計を直接確認）で発覚・修正済み。**教訓: 同じガード条件が複数箇所にコピーされていないか、grepで洗い出してから直すこと**
  - 新イベント種別`hpToAp`をP4の`applyEventsForTick`・P6の`applyEventHeadless`双方に追加（HPバー更新・繰越計算に反映）
  - `ROUND_BASE_CREDITS`に`4: 600`を追加してR3→R4の進行ブロックを解除。R4終了後は`advanceToNextRound()`が「次のラウンドへ」ではなく試合終了メッセージを出す（§1: 4Rは必ず即時終了条件で決着＝試合そのものの結着）
- **P6: ラウンド進行**（`index.html`。R1→R2→R3→R4のクレジット進行まで実装）
  - **[2026-08-23修正] 二重購読レースバグ**: `enterRoom()`が新しいルームを購読する前に古い購読を解除していなかった。リロード時の自動復帰(`tryAutoRejoin`、非同期)と手動での「参加する」がタイミング次第で競合すると、`onValue`購読が2本同時に生きた状態になり得た。両方が`handleRoomUpdate()`で同じグローバル状態(`opponentOnline`等)を更新するため、古い（既にホストが切断済みの）ルームのスナップショットが後から届いて正しい状態を上書きし、「相手待ち」のまま止まって見える不具合になっていた（UC実機テストで発覚。キャッシュ問題だと最初誤診断したが別物だった）。`enterRoom()`冒頭で必ず`unsubscribeRoom()`を呼ぶよう修正し、`tryAutoRejoin()`側も「その間に手動操作で`roomCode`が既にセットされていたら何もしない」ガードを追加。意図的に再現するテスト（旧ルームのホストを切断させた状態で自動復帰と新規参加を競合させる）で修正を確認済み
  - **[2026-08-22修正] 切断・リロード復帰は`sessionStorage`を使用**（旧: `localStorage`）。localStorageは同一オリジンの全タブで共有されるため、1人が2タブを開いて両陣営を1人でテストプレイすると、後から開いたタブが先のタブと同じ役割に自動復帰してしまうバグがあった（UC指摘で発覚）。sessionStorageはタブ単位で独立（同じタブでのリロード追従はそのまま維持）なので、2タブでの1人プレイが正しく動作するようになった。実機検証済み（タブ1でルーム作成→タブ2で新規参加→タブ1リロードで同じ役割に復帰、を確認）
  - **[2026-08-19] R1プールのランダム抽選**: 初期プール8体から`pickRandomPool(seed, 6)`で6体を共有シードで抽選（`ROUND_POOL_SIZE`）。オンラインはホストが`createRoom()`時に`poolSeed`/`pool`をFirebaseへ書き込み、`handleRoomUpdate()`で両クライアントに反映。オフラインはページ読み込み・全リセット時に`Date.now()`シードでローカル抽選。R2以降はプール構成（§10未確定#2）が決まるまで暫定的に全8体を表示
  - **勝敗解釈** `interpretRoundOutcome(battleResult, unitsAtStart, basesAtStart)`: `simulateBattle()`（P3）はtickループを機械的に最後まで回すだけで§6-1の即時終了判定をしない設計だったため、ここでログを先頭tickから再生し直し、即時終了条件（全滅／拠点陥落）が最初に成立したtickを「本当の決着」とみなす。ヘッドレス版イベント適用は`applyEventHeadless()`（P4の`applyEventsForTick`のDOM非依存版）
    - 全滅・拠点陥落とも成立せずtick終端まで到達した場合は§6-2の4段階タイブレーク（拠点総HP→そのRの与ダメージ→キャラ総数→引き分け）
    - 同時陥落（§6-1）: 両者が同一tickで初めて拠点を1個失った場合のみ勝利条件を「全陥落」に切替えて続行。実装は`escalated`フラグで表現
    - 行動力繰越（§5-4）はこの「本当の決着tick」時点の残行動力に対して`computeCarryover()`を再適用（早期打ち切りでない自然終了時は`simulateBattle()`自身の最終状態をそのまま使う）
  - **強化★2/★3（[2026-09-05] 後に撤廃・下記「現在の状態」参照。再実装する場合の参考として残す）**: `purchaseCount[player][charId]`（購入累計、購入では減らない）から`starLevelOf()`で星を動的算出し、`getCharacter(charId, owner)`が★反映済みの実効ステータスを返す（owner省略時は基礎値＝購入カタログ表示用）。差分値は`CHAR_UPGRADES`テーブル（設計書§9のとおり）。UC確認済み: 2体目購入時点では新規ユニットとして配置キューに入り、**配置した時点で**そのプレイヤーの同キャラ全ユニット（既存＋新規）が★2になる
  - **ラウンド遷移** `advanceToNextRound()`: 生存ユニット・生存拠点（HP込み）を次ラウンドの初期配置状態として引き継ぎ、クレジットは「現在値（＝繰越）＋R2基礎額500＋勝者ボーナス200」。購入キューはリセット。罠は`persistentTraps`にラウンドを跨いで保持（★[P6簡略化] 罠設置はログに専用イベントが無いため即時終了時点での正確な再構築ができず、常に`simulateBattle()`のフル実行結果の罠を引き継ぐ。早期打ち切り時に実際よりわずかに罠が多く見える可能性がある程度の誤差）
  - **再配置**（設計書§7）: `RELOCATE_LIMIT`（R2=2体など）。そのラウンド開始時点で盤上にいたユニット（`roundStartUnitIds`）のみが対象。ユニット選択パネルの「再配置する」ボタン→`mode="relocateUnit"`→自陣内クリックで移動。新規購入ユニットの配置は体数無制限（対象外）
  - **P5との連携**: `purchaseCount`もFirebaseの`placements/{role}`に同期（相手の★判定に必要）。ラウンド進行はホスト/ゲストどちらも自分の`roundOutcome`を独立にローカル計算し独立に「次のラウンドへ」を押す設計（決定論なので両者が同じ結果に収束する。同期の待ち合わせは不要）。ラウンド送り時は`room.status`を"placing"に戻し自分の`ready`をfalseに書き込む

## 忘れてはいけないコア仕様（要約）

- 1移動＝行動力1、1攻撃＝行動力1（＋攻撃消費N）。攻撃範囲は基本周囲1マス
- tick制: tickごとに行動可能キャラをシード乱数でシャッフルし1体ずつ1アクション。ダメージ即時適用・死亡即除去
  - **[2026-09-05]** シャッフルは「守る以外（ランダム順）」→「守る（この中もランダム順）」の2段階。同一tick内で守る以外の行動結果（移動・撃破等）が確定した後の盤面状態を見て守るキャラが判定する
  - **[2026-09-05]** 終了条件を厳密化: 全員行動力0、または**全員が「stay」（停止・行動力消費なし）しか起きなかったtick＝盤面がこれ以上動かない手詰まり**を検出したら即座に終了するようにした。50tick上限の安全装置は不要と判断し廃止（UC確認済み）
- キャラ思考: ①範囲内に敵→攻撃（最近接、同距離ランダム）②目的へ1マス移動（壁・全キャラが障害物）③動けない→停止（消費なし）
- 指示（目的）は4種: 敵拠点へ最短 / この場を守る / 敵キャラを追う / 指定マスへ。交戦スタンスはない（範囲に入れば必ず交戦）
- **[2026-09-05仕様変更]** 「守る」は移動しないだけで、範囲内に敵がいれば他の目的と全く同じ「攻撃」判定を行う（連携・押し込み/引き寄せ・毒付与も通常通り発動。旧仕様の「被弾時のみ割り込みで反撃」は廃止）。ただしそのtick内では守る以外の目的のキャラより行動順位が低い。残行動力÷2切り上げを次Rに追加繰り越し（基本値の2倍上限、全ユニット共通）
- 即時終了: 全滅 / 拠点1個陥落（同時陥落時のみ2個陥落に変更）。1〜3Rラウンド勝敗: ①拠点総HP ②そのRの与ダメージ（Rごとリセット・拠点含む・オーバーキル除外）③キャラ数 ④引き分け
- 4R: 行動力切れ後はHP10→行動力1に変換して継続
- 配置は秘匿（盤上の既存キャラと前R情報のみ見える）。**前ラウンドから引き継いだユニットは配置フェイズ中も位置を一切変更できない**（再配置の仕組みは2026-09-06に完全撤廃）。今ラウンドで新規購入・配置したユニットのみ、自陣内で何度でも移動・返却できる
- **[2026-09-05] 強化（★2/★3）システムは一旦撤廃**。全キャラ常に基礎ステータスのみ。過去に「同キャラ購入で自動強化」「盤面上で同キャラ2体を手動合成」の2方式を実装したが、しっくり来ずどちらも取りやめた。詳細は「現在の状態」セクションの記録を参照（将来また検討する可能性あり）
- 「追加攻撃」は「攻撃」ではない: 連携は連鎖しない・押し込み不発・毒の新規付与なし。ただし既存の毒ダメージ+10は発動する
- スキル17種の詳細裁定は設計書 §8 を必ず参照

## コーディング規約

- 作業言語・UI・コメントは日本語
- テストモード用コードは `// [TEST]` コメントを付け、リリース時に一括削除できるようにする（ダイフゴーと同じ規約）
- Firebaseのデータ構造は実装開始時に設計し、本ファイルに追記する

## 実装フェイズ計画

大きなブロック単位で進め、フェイズ完了ごとにUCへ確認する（細かい確認は最小限に）。

- **P1: 盤面基盤** — ヘックス描画、座標系、壁マス、拠点（固定位置に自動配置）、自陣（拠点周辺3マス）の判定
- **P2: 配置フェイズ（ローカル）** — キャラ購入UI、配置、指示（目的4種＋指定マス・向き指定）、クレジット管理
- **P3: 戦闘シミュレータ** — tick制ロジック、経路探索、スキル17種、「守る」の行動順位制御、イベントログ出力。**UIなしで動くこと**（コンソールテスト可能）
- **P4: 戦闘演出** — イベントログ再生、移動/攻撃/スキル/死亡/ダメージ数字の表示、再生速度変更
- **P5: Firebase同期** — ロビー/待機室、マッチング、秘匿配置の同期、シード共有、切断処理
- **P6: ラウンド進行** — 勝敗判定、クレジット進行、強化（★2/★3）、再配置上限、引き継ぎ処理
- **P7: 4R特殊処理・テストモード・調整** — HP→行動力変換（**実装済み**）、テストモード（ラウンドジャンプ・クレジット任意設定等 `// [TEST]`。未着手）、バランス調整（未着手）

## UC向けUI（ルーム参加中は開発用表示を隠す）

UCから「デバッグ用の要素があるとUIの精査ができない」との指摘（2026-08-22）を受け、`setOnlineModeUI(active)`でルーム参加中（`active=true`）は以下をまとめて隠す/文言を差し替える設計にした。オフライン単体テスト時は従来どおり全部表示される。

- `#playerSelectPanel`（操作プレイヤー手動切替）: 非表示
- `#devToolsPanel`（全てリセットボタン・座標表示チェックボックス）: 非表示
- `#pageSubtitle`: 「盤面基盤...（開発テスト用）」→「2人対戦オンライン・オートバトラー」に差し替え
- `#battlePanelTitle`: 「戦闘（P3+P4テスト）」→「戦闘」に差し替え

**新しく開発用UI要素を追加するときは、ルーム参加中に隠す/文言を変えるべきか毎回検討し、`setOnlineModeUI()`に追記すること。** `document.body.innerText.includes('テスト')`でルーム参加後に「テスト」の文字列が残っていないか確認できる。

## 配置画面v2: 盤面中心・上下横帯レイアウト（2026-08-23実装）

UCから「自分が選んだキャラを上・横向き、購入可能キャラを下・横向き」との要望を受け、配置画面の3カラム構造をやめてTFT風の縦積みレイアウトに刷新。Artifactでモックアップ（https://claude.ai/code/artifact/4bf17d9b-9e96-49ee-a964-eefba8534d0e）をUC確認してから組み込み。スキル表示は「見やすくしたい」との追加要望でタグ形式（複数スキルは折り返し）に。

- 縦構成: 開発用オプション（⚙折りたたみ）→ 上部バー（ラウンド・拠点HPバー・クレジット）→ **上段:配置待ち（横スクロールカード列）** → 盤面（横幅いっぱい） → **下段:購入可能キャラ（横スクロールカード列）** → 下部アクションバー
- キャラ未選択時は下段=購入可能キャラ（`#shopStrip`）、盤面のキャラを選択すると下段が指示パネル（`#unitEditPanel`、`.inspector-strip`）に**排他的に切り替わる**（`renderUnitEditPanel()`が両方のdisplayを制御）
- キャラカード（`.hcard`）: アイコン1文字（`CHAR_ICON`定数）＋名前＋価格＋❤⚔⚡ステータス＋スキルをタグ形式で折り返し表示（`.h-skill-tag`）。買えないキャラは`.unaffordable`で薄く表示しクリック無効
- 拠点HPは`renderBaseHp()`で実バー表示（拠点は常に2個・各300HP前提。陥落した拠点は配列から消えるので生存分のhp合計をそのまま「600に対する割合」として描く）
- 指示（目的4種）はラジオボタン→`.purpose-chip`ボタン列、向きは横一列の矢印→`.facing-row`（3×3グリッド、実際は6方向のみ配置）
- 実機検証済み: ショップ購入→配置待ちカード表示→盤面クリックで配置→指示パネルへの排他切替→選択解除で購入画面に復帰、の一連の流れ。複数スキル持ち（アーチャー・クレリック）のタグ折り返し、向き設定（スナイパー）の表示。コンソールエラーなし
- **[2026-09-05] 指示パネルの位置を最上段に変更**。UC要望「指示の画面を上に持ってきたい。購入キャラの上にもってこれる」への対応。当時から購入UIはポップアップ化・購入ボタンは上部バー常設に変わっており（本ファイル前掲のセクション参照）、このレイアウト説明はその後の変更を反映していない点に注意。現在のDOM順は「開発用オプション → 上部バー（購入ボタン込み） → **`#unitEditPanel`（指示パネル、ユニット未選択時は`display:none`）** → `#queueList`（配置待ち/配置済みカード列） → 盤面」。`renderUnitEditPanel()`のロジック（表示/非表示の切替条件）自体は変更なく、`index.html`内でのHTML要素の並び順だけを入れ替えて実現した。実機検証済み（ユニット選択時に上部バー直下にパネルが表示され配置待ちカード列がその下に来ること、選択解除で元のレイアウトに戻ることを確認。コンソールエラーなし）
- **[2026-08-23追記]** UC指摘「購入して配置すると上段から消える。配置済みのキャラも記載してほしい、配置していないキャラはハイライトで目立たせること」への対応で`renderQueueList()`を拡張。上段には①配置待ち（`queue[currentPlayer]`、`.hcard.queued`＝アクセント枠＋背景色＋「未配置」バッジで強調）と②配置済み（`units.filter(u=>u.owner===currentPlayer)`、`.hcard.placed`＝破線＋半透明で控えめ表示、`配置済み・${PURPOSE_SHORT}`）を両方並べる。配置済みカードをクリックすると盤面上のユニットをクリックしたのと同じ`selectUnitForEdit()`が呼ばれ指示編集パネルが開く。実機検証済み（購入2体→1体配置→両方が正しいスタイルで並ぶ・配置済みカードクリックで編集パネルが開く、コンソールエラーなし）
- **[2026-09-05] 購入UIを下段の横スクロール帯からポップアップに変更**。UC要望「購入画面をポップアップ的に出したい、下に置くのはなし」への対応。旧`#shopStrip`（`.strip.bottom`）を廃止し、未選択時は盤面下に`#shopCtaRow`（「＋ キャラを購入」ボタン）だけを表示。押すと`#shopScrim`（背景スクリム）+`.shop-modal`（中央ダイアログ、グリッドで`#charList`を表示）が開く。閉じるボタン・スクリムクリック・Escキーで閉じる。購入してもモーダルは閉じず連続購入できる（`buyCharacter()`→`render()`→`renderCharList()`で都度クレジット表示を更新、モーダル内`#creditsLabelShop`にも反映）。ユニット選択時の指示パネル切替ロジック（`renderUnitEditPanel()`）は`shopStrip`だった参照を`shopCtaRow`に差し替えただけで、挙動は従来通り排他表示。Artifactでモックアップ（https://claude.ai/code/artifact/42d169a1-c442-4ab3-9e22-1d911e5b53cb）をUC確認してから組み込み。実機検証済み（購入モーダルの表示・複数購入・閉じる・配置後の指示パネル排他切替、コンソールエラーなし）
- **[2026-09-05] 購入の返却（取り消し）機能を追加**。UC指摘「一回購入すると決まってしまうのはよくない、返却ボタンがあってもいい」＋「1ラウンドで置いた奴は次のラウンドでは返却できないように」への対応。
  - `returnQueuedUnit(player, qid)`: 配置待ち（未配置）キューからの返却。クレジット全額返金＋`purchaseCount`を1戻す（星判定に使うため）。購入キューはラウンドをまたがない仕様（ラウンド遷移時にリセット）なので無条件で返却可。上段の配置待ちカード（`.hcard.queued`）に「返却」ボタン（`.h-return-btn`）を追加、クリックは`stopPropagation()`でカード本体の配置モード開始と分離
  - `returnPlacedUnit(unitId)`: 盤面に配置済みのユニットの返却。ユニット削除＋クレジット返金＋`purchaseCount`減算。ユニット指示編集パネル（`renderUnitEditPanel()`）内、既存の「再配置」ボタンと**排他的**に表示: `roundStartUnitIds`（そのラウンド開始時点で盤上にいた＝前ラウンドから引き継いだユニット）に含まれる場合は再配置ボタンのみ、含まれない場合（＝今ラウンドで新規購入・配置したユニット。R1は全ユニットがこちら）は「返却する（クレジットNを返金）」ボタンのみ表示。この判定は既存の再配置上限ロジックと同じ`roundStartUnitIds`をそのまま流用しているため、ラウンド跨ぎでの返却不可はコード変更なしで自然に成立
  - 実機検証済み: R1で購入→キュー返却（全額返金・キューから消える）／購入→配置→配置済み返却（盤面・キューから消え購入ボタン表示に復帰）／R1で配置したユニットがR2では再配置のみ（返却ボタンなし）／R2で新規購入・配置したユニットには返却ボタンが出る、の4パターンをコンソールエラーなしで確認
- **[2026-09-05] ステータス表記を目盛りバー化**。UCが提示した参考画像（ポートレート＋名前＋行動力/攻撃力を平行四辺形の目盛りバーで表示＋スキル欄）を基に、Artifactで3案（A:購入モーダル用フル情報／B:上段ストリップ用コンパクト／C:比較用の現行踏襲）をモックアップ提示 → **案B（コンパクトなバー表記を購入モーダル・上段ストリップ両方に統一適用）**をUC選択。
  - `STAT_MAX`（HP/攻撃力/行動力それぞれ全キャラ中の基礎値の最大値）を基準に`STAT_SEGMENTS=6`目盛りの相対バーを`statBarRow()`/`statBarsHtml()`で生成（星強化分は考慮しない簡易スケール。基礎最大値を超えても見た目は満タンのままで表示は破綻しない）。旧`.h-stats`（❤⚔⚡の数値羅列）を廃止し、HP/行動力/攻撃力の3本の目盛りバー＋数値に統一
  - スキル表示も`skillPillsHtml()`に統一（旧`.h-skill-tag`→`.skill-pill`にリネーム。中身・挙動は同じ、スキルなし時のグレー表示・★強化案内ピルも維持）
  - 適用箇所: 購入モーダルの`renderCharList()`、上段ストリップの配置待ち/配置済み両方の`renderQueueList()`。返却ボタン・未配置バッジ・指示パネル等の既存機能に変更なし
  - 実装時のバグ: 目盛りバー導入直後、`.stat-label`の幅が3文字ラベル（「行動力」「攻撃力」）に対して狭く、「行動\n力」のように折り返してしまった。幅を2.6em→3.1em＋`white-space:nowrap`に修正して解消
  - 実機検証済み（Chrome）: 購入モーダルのカード表示・購入→配置待ちカードのバー表示・配置→配置済みカードのバー表示・指示パネルとの整合性、コンソールエラーなし
- **[2026-09-05] 購入ボタンを盤面下から上部バー（残クレジットの隣）に移動**。UC要望「購入画面（ボタン）を残クレジットの横にしてほしい」への対応。`#openShopBtn`を`.placement-topbar`内の`.credits-pill`直後に常設し、ユニット選択有無に関わらず常時表示に変更（旧`#shopCtaRow`は廃止）。下段はユニット選択時の指示パネル（`#unitEditPanel`）専用になり、未選択時は何も表示しない（`renderUnitEditPanel()`から`shopCtaRow`の表示切替を削除するだけで対応）。ボタンのスタイルも中央大型ボタン（`.shop-cta`）からバー内に収まる小型ピル（`.shop-cta-topbar`）に変更。実機検証済み（購入→配置→ユニット選択で指示パネルと購入ボタンが共存表示されることを確認、コンソールエラーなし）
- **[2026-09-05] スキルのマウスオーバー説明を追加**。UC要望「購入画面でマウスオーバーしたときスキルの説明が出るようにしてほしい」への対応。設計書§8の効果文を基準に`SKILL_DESCRIPTIONS`（スキル基礎名→説明文、末尾のNはプレースホルダーのまま保持）を定義し、`skillDescription(skillStr)`が実際のスキル文字列（例: `射撃5`）末尾の数値を正規表現で抽出して説明文中の`N`を実数値に置換する。`skillPillsHtml()`が生成する各`.skill-pill`にネイティブの`title`属性として設定するだけの実装（カスタムツールチップは実装せずブラウザ標準機能に委譲）。`skillPillsHtml()`を使う購入モーダル・上段配置待ち/配置済みカードすべてに自動的に反映される。`.skill-pill`に`cursor:help`を追加しホバー可能であることを視覚的に示した。実機検証: `title`属性の内容が全キャラ・全スキル（★強化で入れ替わる`射撃7`「追従2`等含む）で正しく生成されることをDOM経由で確認（ネイティブtitleツールチップの表示自体はCDPスクリーンショットには写らないため、属性値の正しさで検証）。コンソールエラーなし
  - **[2026-09-05追記] 表示位置をスキルピル個別→カード全体に変更**。UC指摘「スキルの詳細はキャラにカーソルを当てた時点で全て出してほしい。スキルの上ではなく」への対応。`allSkillsTooltip(skills)`が`スキル名: 説明文`を改行区切りで結合した文字列を作り、各`.hcard`要素自体の`title`に設定（`skillPillsHtml()`側の個別`title`は削除、`.skill-pill`の`cursor:help`も役目が無くなったため削除）。購入モーダル・上段配置待ち/配置済みカードすべてに適用。実機検証済み（各カードの`title`に全スキル分の説明が改行区切りでまとまっていること、個別ピルには`title`が付いていないことをDOM経由で確認。コンソールエラーなし）

- **[2026-09-05] 指示（目的）UIの変更2点**。UC要望「基本は敵の拠点に向かうが最初にセットしてある指示にしよう。次は指定マスへ向かうを選択したら即座に目標マスを選べるようにしよう。目標マスを指定ボタンを削除」への対応。
  - **デフォルト指示を「守る」→「敵拠点へ最短で向かう」に変更**。`handlePlaceUnit()`の新規配置時`purpose`初期値を`"guard"`→`"rush"`に変更。配置直後のステータスメッセージも「指示（目的）を設定してください」→「（指示: 敵拠点へ最短で向かう）。必要なら変更してください」に変更
  - **「目標マスを指定」ボタンを廃止し、「指定マスへ向かう」チップを押した瞬間に即座に目標マス選択モードへ入る**ように変更。`setUnitPurpose(unitId, purpose)`が`purpose==="goto"`の時、既存の`targetHex`を必ずクリアした上で`mode="setTarget"`・`selectedUnitId`をセットして即座に選択待ちにする（旧`startSetTarget()`関数は不要になったため削除）。指示編集パネルには目標マス選択用のボタンは置かず、既に目標が設定されていれば`目標: colX,rowY（チップを押し直すと選び直せます）`という非インタラクティブなヒント文だけを表示する。再度「指定マスへ向かう」チップを押せば`targetHex`がリセットされ再び選択モードに入る（選び直しの唯一の手段）
  - 実機検証済み: 配置直後に「敵拠点へ最短で向かう」チップがアクティブになっていること／「指定マスへ向かう」チップを押した瞬間に盤面クリック待ちになること／盤面クリックで目標が設定されヒント文に反映されること／チップを再度押すと選び直せること、をコンソールエラーなしで確認

- **[2026-09-05] 配置済みユニットを何度でも移動できる「配置位置を変える」機能を追加**。UC指摘「一度配置をしたら動かせないのがやだ、何度でも配置位置を変えられるようにしよう」への対応。今ラウンドで新たに購入・配置したユニット（`roundStartUnitIds`に含まれない）専用の、回数制限なしの移動機能。
  - 新しい`mode`値`"moveUnit"`・`startMoveUnit(unitId)`・`handleMoveUnit(col,row)`を追加。バリデーション（壁不可・自陣内のみ・空きマスのみ）は既存の`handleRelocateUnit`と同じだが、**`relocatedThisRound`には一切触れない別カウントの実装**にした。前ラウンドから引き継いだユニット（R2以降）向けの回数制限つき「再配置」（設計書§7で規定済みのバランス調整用ルール）とは完全に独立しており、こちらの機能を追加しても再配置の残り回数には影響しない
  - 指示編集パネルの「今ラウンドで新規配置したユニット」の分岐（既存の返却ボタンと同じ分岐）に「配置位置を変える」ボタンを追加。前ラウンドからの引き継ぎユニットの分岐（再配置ボタン）には追加していない
  - 実機検証済み: 今ラウンドで配置したユニットに「配置位置を変える」ボタンが出て、2回連続で異なるマスへ移動できること／R2に持ち越したユニット（配置済みカードから選択）は引き続き「再配置する（残りN体）」のみで新ボタンは出ないこと、をコンソールエラーなしで確認

- **[2026-09-05] 「配置完了」ボタンを下部アクションバーから上部バーへ移動**。UC要望「配置完了は右上に置こうか」→直後に「残クレジットと、HPの間にしよう」で位置を再指定。`#readyRow`（`readyBtn`+`readyStatusLabel`）を`.placement-topbar`内、`statusMsg`の直後・`credits-pill`の直前（＝base-hp-groupと残クレジットの間）に配置。オンラインルーム参加中のみ表示する制御は従来通り`setOnlineModeUI(active)`が担い、対象を`onlineBattleNote`から`readyRow`に変更しただけ（`active ? "inline-flex" : "none"`）。`statusMsg`の`flex:1`により、readyRow以降（credits-pill・購入ボタン）は引き続き右寄せでまとまる。下部の`onlineBattleNote`には案内文（「配置が終わったら上部の『配置完了』を押してください」に更新）だけが残る。実機検証済み（`readyRow`がHPバーと残クレジットの間に表示されること、ボタンクリックが機能すること、コンソールエラーなし）

## 配置フェイズ画面・戦闘画面の分割（2026-08-22実装）

UCから「配置画面のUIを変えよう、配置画面と戦闘画面をわける」との依頼を受け、`#layoutRoot`直下を`#placementScreen`／`#battleScreen`の2つの排他表示スクリーンに分割した。Artifactでモックアップ（https://claude.ai/code/artifact/932d1439-2a4b-4920-8fbb-1f3c286ae139）をUC確認してから組み込み。

- `showPlacementScreen()` / `showBattleScreen()`で切替。`enterBattleMode()`/`exitBattleMode()`（P4/P6既存関数）の中から呼ぶだけで、既存の状態遷移ロジック（オンライン自動開戦・次のラウンドへ等）はそのまま流用
- **盤面svg(`#board`)は1つしかない**（複製していない）。`#boardSlotPlacement`と`#boardSlotBattle`という2つの空divを両画面に用意し、画面切替のたびに`appendChild(svg)`で同じDOMノードを移動させている。SVG自体の中身・イベントリスナーは無傷なので安全
- 配置画面: 上部に折りたたみ式「⚙開発用オプション」（プレイヤー手動切替・全リセット・座標表示・凡例。デフォルト非表示）→ラウンド/拠点HPの帯→3カラム（購入 | 盤面 | 選択中キャラの指示）→下部固定アクションバー（配置完了 or 戦闘開始）
- 戦闘画面: 盤面を大きく左、右サイドにログ＋再生コントロール＋結果／次のラウンドへ
- 実機検証済み（オフライン・オンライン2タブとも）: 配置→戦闘→結果→次ラウンドで配置画面に戻る、の一連の遷移。開発用オプションの折りたたみ動作。コンソールエラーなし

## ロビー画面（独立した入口スクリーン、2026-08-22実装）

UCから「見た目も操作性もUIを整えたい、まずロビーから」との要望を受け、`#lobbyScreen`を`#layoutRoot`（従来の`.layout`）や`#pageHeader`（旧h1/subtitle）と排他表示する独立スクリーンとして実装した。Artifactでモックアップ（https://claude.ai/code/artifact/99b4e192-8bf8-45f5-9d42-e55cfdcd3102）をUC確認してから組み込み。

- **3状態**: `showLobbyScreen("A"|"B"|"C")` / `hideLobbyScreen()`。状態はグローバル`lobbyPhase`（"entry"/"inRoom"/"matched"/"hidden"）で管理
  - A: 未参加（ルーム作成／合言葉で参加）。ヘックス柄の背景＋ゴールドの「AFBE」ロゴ
  - B: 自分だけ参加・相手待ち。「配置完了」ボタンは**ここには置かない**（配置前で意味がないとUC指摘、修正済み）。合言葉・P1/P2接続状況カードのみ
  - C: 相手が接続した瞬間の「マッチ成立」演出（1.1秒）→自動的に`hideLobbyScreen()`
- 開発用に`#devSkipLobbyBtn`（「開発用: ロビーをスキップしてローカルでテスト」リンク）を状態Aの下に設置。押すと`lobbyPhase="hidden"`にしてロビーを飛ばし、従来の手動P1/P2切替デバッグ画面に直接入れる
- 「配置完了」ボタン（`readyBtn`/`readyStatusLabel`）は戦闘パネル内の`onlineBattleNote`に移設済み（ロビーではなく配置フェイズ側に属するため）
- `updateLobbySideRoles()`でP1/P2どちらの役割でも自分側が正しい色（青/赤）になるよう動的に割り当て
- 実機検証済み: 状態A→B→C→本編の一連の遷移、退出での状態Aへの復帰、開発用スキップ、2タブでのマッチング演出まで確認。コンソールエラーなし

- **[2026-09-06] 試合終了ポップアップが盤面を覆い隠す問題を修正**。UC要望「ゲーム終了時に盤面が見られるようにしてほしい」。従来は共通の`.scrim`（画面全体を暗転＋ぼかし）をそのまま試合終了（勝利/敗北/引き分け）ポップアップにも使っていたため、盤面が完全に見えなくなっていた。
  - `#matchResultScrim`だけ`background:transparent; backdrop-filter:none;`で暗転・ぼかしを外し、`justify-content:flex-end`でログ側（右）に寄せて表示するよう変更。盤面（左）は常にはっきり見える状態になる
  - スクリム自体は透明でも`position:fixed;inset:0`のまま画面全体を覆っておりクリックを奪ってしまうため、`#matchResultScrim { pointer-events:none; }`＋`.match-result-modal { pointer-events:auto; }`でポップアップ本体だけクリックを受け取り、それ以外の場所（盤面のズームボタン等）は素通りするようにした
  - shopScrim・rulesScrimなど他のモーダルは`#matchResultScrim`個別指定のため影響なし（従来通り暗転する）
  - 実機検証済み: 試合終了ポップアップ表示中も盤面がはっきり見えること、ズームボタンが問題なく操作できること、「再戦する」ボタンも正常にクリックできることを確認。コンソールエラーなし

- **[2026-09-06] 行動力の繰越上乗せを配置画面に表示＋ルール説明に追記**。UC質問「行動力の引継ぎ上乗せってルールなかったっけ」→実機検証で仕様(設計書§5-4)自体は正しく動作していること確認済み（ソルジャーが「守る」でAP20を温存→次ラウンドで20+10=30回行動できることをログで確認）。ただし配置画面に上乗せ分が一切表示されておらず、それが疑問の原因だった。
  - `renderUnitEditPanel()`の指示パネル（自分のキャラ・相手キャラの閲覧ビュー両方）で、`行動20`だけだった表示を`unit.apCarry`（`advanceToNextRound()`で前ラウンドの繰越分がセットされる）がある場合`行動20+10`のように上乗せ分を併記するよう変更
  - ルール説明ポップアップの「戦闘フェイズ」節に、繰越の仕組み（残り行動力の半分を次ラウンドに上乗せ、上限は基礎の2倍）を追記
  - 実機検証済み: 自分のキャラ・相手キャラどちらの閲覧でも「行動20+10」の表示になること、新規配置キャラ（繰越なし）は従来通り「行動20」のみの表示のままであることを確認。ルール説明ポップアップにも文言が反映されていることを確認。コンソールエラーなし

- **[2026-09-06] 配置フローの快適化＋goto到達キャラの引き継ぎ挙動＋盤面ズーム機能（4件）**
  1. **購入直後に配置待ち状態へ**。「購入したキャラを選択状態にする」。`buyCharacter()`で、キューに追加すると同時に`mode="placeUnit"; selectedQueueEntry=entry;`をセットし、購入後すぐ盤面タップで配置できるようにした（従来はキュー内のカードを改めてクリックする一手間が必要だった）
  2. **配置直後に指示パネルが自動で開く**。「置いたキャラにはすぐ指示が出せるように選択状態に」。`handlePlaceUnit()`で新規ユニット作成後に`selectedUnitId`をそのユニットのIDにセット
  3. **「指定マスへ向かう」が目標到達→次ラウンドでは「拠点を攻める」に**。「目標マスにたどり着き次のラウンドにいったキャラの指示は、その場を守るではなく拠点の攻めるに変更してほしい」。戦闘中に目標到達で自動的に`purpose:"guard"`へ切り替わる仕様（`targetHex`はクリアされず残る）を利用し、`advanceToNextRound()`の引き継ぎ処理で「`purpose==="guard"`かつ`targetHex`が残っている」＝自動切り替え由来と判定して`rush`に上書きする。プレイヤーが手動で「守る」を選んだ場合は`setUnitPurpose()`が必ず`targetHex`をnullにするため区別できる
  4. **盤面のズームイン/アウト機能**。「マップをズームインができるようにしてほしい」。盤面svgは配置画面・戦闘画面で同じ要素を使い回している（`enterBattleMode`/`exitBattleMode`でスロット間を移動）ため、両方の盤面コンテナ右上に共通の`.board-zoom-controls`（－/％表示/＋/リセット）を設置し、`boardZoom`倍率に応じて`svg`の`width`/`height`属性だけを変える（`viewBox`は固定なので内部座標・クリック判定に影響しない）。範囲は60%〜260%、1段階20%刻み
  - 実機検証済み: ①購入直後に「盤面をクリックして配置してください」の状態になっていること②配置直後に指示パネル（このキャラの名前）が自動表示されること③「指定マスへ向かう」が目標到達→次ラウンドで指示が「敵拠点へ最短で向かう」に変わっていること（手動で「守る」を選んだ別ユニットは次ラウンドでも「守る」のまま変わらないことも確認、回帰なし）④配置画面・戦闘画面どちらでもズームボタンでsvgのwidth/heightが変化し、リセットで元に戻ることを確認。コンソールエラーなし

- **[2026-09-06] バグ修正: 「射程ボーナスN」の攻撃範囲がNマスしか無くN+1マスあるべきだった**。UC報告「アーチャーの攻撃範囲って周囲2マスやん、ボーナスで+2だから3マスいけるはず、クレリックも2マスや」。
  - **原因**: `attackRangeCells()`で`射程ボーナスN`スキルを`const n = bonusSkill ? parseInt(...) : 1;`としており、ボーナス値そのものを範囲にしてしまっていた。設計書§8「射程ボーナスN: 攻撃範囲が全方位にNマス広がる」の通り、基礎範囲1マスに**加算**すべきだったのに、加算せず置き換えていた（アーチャー=射程ボーナス2なら本来1+2=3マスのところ2マスにしかなっていなかった）
  - **修正**: `const n = bonusSkill ? 1 + parseInt(...) : 1;`に変更。この関数は配置画面・戦闘画面共通の攻撃範囲オーバーレイと、戦闘シミュレータ本体（`findTargetsInRange`）の両方で使われる唯一の実装なので、見た目だけでなく実際の戦闘判定（何マス先まで攻撃できるか）も同時に直っている
  - 実機検証済み: 同じ配置マスでガーディアン（スキルなし=1マス）・クレリック（射程ボーナス1）・アーチャー（射程ボーナス2）を順に配置し、攻撃範囲オーバーレイの表示マス数がそれぞれ6・17・27（壁による欠けを除けば理論値6・18・36相当）と、狙い通り大小関係になることを確認。コンソールエラーなし

- **[2026-09-06] ロビー画面に「遊び方・ルール説明」ボタンを追加**。UC要望「ルール説明ボタンを最初の画面に追加したい、ルール説明のポップアップが出るようにして、説明のところも作って」。
  - ロビー画面（`#lobbyStateA`、ブランド表示の下）に`#rulesBtn`を追加。クリックで`#rulesScrim`（既存のショップモーダルと同じ`.scrim`パターン）が開き、概要・配置フェイズ・指示4種・クレジット表（ラウンド別基礎額の最新値を反映）・戦闘フェイズ・勝敗条件・キャラとスキルの各セクションを日本語でまとめた`.rules-body`を表示する
  - 閉じる操作は既存モーダルと同様に3通り対応: ✕ボタン・スクリム外側クリック・Escapeキー
  - 実機検証済み: ボタンクリックでモーダルが開く・各セクションが正しく表示される・✕ボタンとスクリム外側クリックの両方で閉じることを確認。コンソールエラーなし
  - **注意**: ルール文面はゲームバランス調整（クレジット額等）のたびに内容が古くなりうる。今後クレジット額や勝敗条件などを変更した際は、`index.html`内の`#rulesScrim`のテキストも合わせて更新すること

- **[2026-09-06] 指示パネル出現時に盤面が左にずれる問題を修正**。UC指摘「右側に指示出るときに少し戦場とかUIが左にずれるのが気になる」。原因は`.placement-body`のgrid-template-columnsを未選択時`1fr`・選択時`1fr 240px`と切り替えていたため、サイドバー出現のたびに盤面側カラムの実効幅が変わり、`.col-board`の中央寄せ位置がずれていた。**常に`1fr 240px`の2カラムを確保**し、パネル自体の表示/非表示（`display:block/none`）だけで切り替えるよう変更（列数は変わらないので盤面の位置は固定される）。JS側の`renderUnitEditPanel()`から`.has-selection`クラスの付け外し処理を削除
  - 実機検証済み: `#boardSlotPlacement`の`getBoundingClientRect()`を選択前後で比較し、`left`・`width`とも完全に一致することを確認。コンソールエラーなし

- **[2026-09-06] バランス調整第2弾: R2〜R4の基礎クレジットを再調整**。UC指示「1R200 2R300 3R400 4R500にしよう」。前回（同日）500/400/600→200/200/300に引き下げたばかりだったが、今回はR1の200からラウンドごとに+100ずつ増える単調増加の形に変更。`ROUND_BASE_CREDITS = { 2: 300, 3: 400, 4: 500 }`（旧: `{2:200, 3:200, 4:300}`）。設計書§7の表も更新済み
  - 実機検証済み: R1(200)→R2(+300=延べ増分300)→R3(+400)→R4(+500)と、各ラウンド境界での増分がすべて意図通りであることを確認。コンソールエラーなし

- **[2026-09-06] 敵キャラのステータス閲覧＋重大バグ修正（押し込みで発動者が対象と一緒に動く）**
  1. **敵キャラをタップするとステータス・残りHPが見えるように**。「タップすると敵のキャラのステータスや残りHPが分かるようにする」。`selectUnitForEdit()`の所有者チェックを撤廃して相手のキャラも選択可能にし、`renderUnitEditPanel()`で`unit.owner !== currentPlayer`の場合は閲覧専用ビュー（肖像・名前・残りHP/最大HP・攻撃力・行動力・スキルピル）を表示するよう分岐。指示チップ・移動/返却ボタン・向きピッカーは一切出さず、「相手の指示（目的）は秘匿のため見えません」という注記のみ表示する。ついでに自分のキャラ側の表示も、今までベースの最大HPしか出ていなかった（実際に前ラウンドで削れたHPが反映されていなかった）のを`現在HP/最大HP`表示に修正
  2. **重大バグ修正: 押し込み/引き寄せが発動すると、発動者(attacker)のマーカーが対象と一緒に動いてしまう**。UC報告「ガーディアンが押し込み発動したら一緒についていった、それはおかしい」。原因は自分（今回のセッション）が以前に仕込んだ回帰: `applyEventsForTick`の`move`/`push`/`pull`/`followMove`共通caseが`findDisplayEntity(ev.unitId != null ? ev.unitId : ev.targetId)`という判定だった。これは「push/pullのログにunitIdが無い」ことを前提にした分岐だったが、スキルログカード機能（同日実装）のために押し込み/引き寄せ/毒のログへ`unitId: attacker.id`を追加した際、この分岐が意図せず**push/pullでも発動者側を動かしてしまう**ようになっていた。move/followMoveとpush/pullを別caseに分離し、push/pullは常に`targetId`側を動かすよう修正
  - 実機検証済み: ①P2のガーディアンをP1視点でタップ→残りHP・攻撃力・行動力・スキルピルは見えるが指示関連のUIが一切出ないことを確認、②ガーディアンvsソルジャーの戦闘で押し込み発動tick（t14）にガーディアン自身の「移動」ログが出ないこと・押し込み前後でガーディアンとソルジャーの座標が全く別の場所（P1側/P2側）のまま乖離していること（=同じ座標にテレポートしていない）をログとDOM座標の両方で確認。コンソールエラーなし
  - **教訓**: ログ表示用にイベントデータへフィールドを追加する時は、そのフィールドを別の場所（アニメーション処理など）で分岐条件に使っていないか必ず確認すること。今回は同じセッション内で自分が原因を作り自分で気づかず、UCの実機報告で発覚した

- **[2026-09-06] 配置画面まとめ対応（UCから6件の要望・不具合報告を一括対応）**
  1. **指示パネルを盤面右側のサイドバーに移動**。「指示は盤面の右側のアイドルスペースにボタン置けないかな」。盤面(`.col-board`)は横幅に余裕があり右側が常に空いていたため、`#unitEditPanel`を`.col-board`と並べて`.placement-body`（新設のgridラッパー、`display:grid`）に入れ、ユニット未選択時は1カラム（盤面が中央いっぱいに広がる）・選択時だけ`.has-selection`クラスで`1fr 240px`の2カラムになりサイドバーとして現れるようにした。中身（指示チップ・返却/移動ボタン等）も横並びから縦積みに再構成
  2. **キャラ選択中の空きマスタップで直接移動できるように**。「キャラを選択して、マス目をタップしたら配置変更できるようにしたい」。以前は「配置位置を変える」ボタンを押してmoveUnitモードに入る必要があった。`onHexClick()`で、選択中のユニットが自分の・今ラウンド新規配置（`roundStartUnitIds`に含まれない）ユニットなら、空きマスへの直接タップでも`handleMoveUnit()`が動くようにした（ボタン自体も残しているので両方使える）
  3. **勝者クレジットボーナス+200を撤廃**。「勝利時の傾斜は消そう　クレジットボーナスはなし」。`advanceToNextRound()`の`credits[p] += roundBaseCredits + (outcome.winner === p ? 200 : 0)`から`+200`部分を削除。設計書§6-2・§7も更新
  4. **向き変更を盤面上の円形ピッカーで**。「向きを変えるキャラは選択したら向きを決めるサークルがそのキャラの周りに出て、それで向きを選択できるように」。射撃N持ちキャラを選択中は、`render()`のユニット描画ループで6方向すべてをクリック可能な小さい点として周囲に配置し、現在の向きだけ大きく塗りつぶして強調する方式を追加（サイドパネルの矢印ボタン列は従来通り残し、どちらでも変更可能）
  5. **バグ修正: 戦闘中のHPバーが次ラウンドの戦闘開始時にフルに見える**。「戦闘中のHPバー、次のラウンドの戦闘時にフルに見えている、から継続したバーにして」。原因は2つの複合: ①`createBattleMarker()`はHPバーを常に満タン幅で生成し`updateMarkerHpBar()`を呼ぶまで実際の割合に更新されないが、`restartPlayback()`がマーカー生成後にこの呼び出しを忘れていた（このtickで一度もダメージを受けなければ試合終了まで満タン表示のまま）。②`initBattleState()`の拠点初期化で`maxHp`が`baseHp`（常に300）ではなく`b.hp != null ? b.hp : baseHp`という誤った条件になっており、繰越HPがある場合`maxHp`もその値になってしまい拠点のHPバーの割合が常に1.0（満タン）になっていた。両方修正
  6. **バグ修正: 配置フェイズで相手の指示が見えてしまっている**。「前の、相手の指示が見えてしまっている」。`render()`のユニット描画ループが`for (const unit of units)`と陣営を問わず全ユニットを対象にしていたため、目的地への点線＋×マーク（goto指示）・指示バッジ（突/守/追/指）・向きインジケーターの3つが相手のユニットでも表示されてしまっていた。`isOwn = unit.owner === currentPlayer`を追加し、この3要素は自分のユニットのみ表示するよう修正（位置・見た目・HPバーは仕様通り相手のキャラも表示したまま）
  - 実機検証済み: ①②サイドバーの表示/非表示切り替えとレイアウト崩れ無し③空きマスタップでの移動成功④スナイパー選択時に6方向ピッカーが表示され、クリックで向きが切り替わりサイドパネルの矢印表示とも一致⑤R1でガーディアン同士を交戦させ被弾→R2開始時、盤面のマーカーが被弾済みの幅（25.2/28,19.6/28）で表示されることを確認（修正前は必ず満タン28/28だった）。さらにR2で拠点ダメージ(-20)発生→R3開始時、拠点マーカーが26.1/28で表示されることも確認⑥P2でユニットを配置・指示変更後、P1視点に切り替えるとそのユニットの指示バッジが表示されない（総バッジ数が自分のユニット分のみ）ことを確認。すべてコンソールエラーなし（拡張機能由来の既知の無害なメッセージのみ）

- **[2026-09-06] 配置画面: 指示マーク（突/守/追/指）がHPバーと重なって見づらい問題を修正**。UC指摘「盤面の指示が上に出てるとHPバーとかぶって見づらい」。
  - **原因**: `render()`内で、ユニット上部の指示マーク（`y - HEX_SIZE*0.8`）とHPバー（`appendMarkerHpBar`、`y - HEX_SIZE*0.62 - 8`付近）がどちらも「マーカー直上」の狭い領域を取り合っており、常に重なっていた（HPバー機能は2026-09-06に後から追加されたが、既存の指示マークの位置とぶつかることを考慮していなかった）
  - **検討過程**: 最初は「マーカー下部に移動」を試したが、縦に隣接配置すると今度は**下のキャラの顔と衝突する**別の問題が発生することに気付き却下。最終的に、毒状態アイコン等で既に使っている「マーカーの角に小さいバッジを固定表示する」パターンを踏襲し、**マーカー右下角に円形の小バッジ**（背景色+金色枠、中に1文字）として表示する方式にした。マーカー中心からの距離が固定（半径×0.72のオフセット）のため、HPバー・隣接ユニットのどちらとも幾何学的に衝突し得ない
  - 実機検証済み: 縦に隣接する2マスにユニットを配置し、指示を変更した状態でSVGの各要素の座標を直接比較してHPバー・隣接ユニットとの重なりが無いことを数値確認、さらに拡大スクリーンショットでバッジが視認しやすく表示されることを確認。コンソールエラーなし
  - 既知の残課題（軽微・未対応）: 射撃N持ちキャラの「向き」ウェッジ（`HEX_SIZE*0.9`の距離）が、たまたま右下方向を向いている場合にこの新バッジと近接する可能性がある。発生頻度が低く見た目への影響も軽微なため今回は様子見

- **[2026-09-06] バグ修正: 同じキャラを複数体出すとログで見分けが付かない**。UC報告「ログを見ると[t15]〜[t18]で同じ『ランサー(P2)』が毎tick移動と拠点攻撃を両方していて、同じマスに止まってるのにおかしい」。
  - **原因**: 実際にはシミュレーション側のバグではなく、P2側にランサーが2体いて、片方（拠点に到達済み）が毎tick攻撃、もう片方（まだ移動中）が毎tick移動していただけだった。ただしログの表示名が`ランサー(P2)`のみで陣営は分かっても**どの個体か**が分からず、1体が矛盾した行動をしているように見えていた
  - **修正**: `unitIndexSuffix(entity)`を新設。同じ所有者・同じキャラ種が盤上に複数体いる時だけ、配置順（unit idの連番部分でソート）に基づき「ランサー:1」「ランサー:2」のように通し番号を付ける（1体しかいない場合は今まで通り番号なし）。`entityLabelHtml()`（通常ログ行）とスキルカードの名前表示の両方に適用。番号は配置順で固定されるため、途中で片方が死んでも振り直されない
  - 実機検証済み: ランサー2体を配置した戦闘で「ランサー:1(P1)」「ランサー:2(P1)」がログ上で正しく区別されること、ソルジャー1体のみの戦闘では従来通り番号なしで表示されることを確認。コンソールエラーなし

- **[2026-09-06] バトルログの演出強化（スキル発動を肖像カードで強調）**。UC要望「バトルログをお洒落にしたい。移動はそこまで目立たせなくていいけど、スキルは何が使われているかわかりやすくしたい」への対応。他ゲームの戦闘ログ・スキル演出パターン（インワールド完結型／カットイン型／メッセージウィンドウ型／高密度ログ型／ハイライト混在型／予告結果型）をArtifactで比較した上で、UCが「ハイライト混在型」（格闘ゲームのコンボ表示に近い発想）を選択。
  - 対象スキルは盤面のふわっとラベル表示（`spawnSkillLabel`）と同じ7種: `linkedAttack`（近接/遠距離連携攻撃）・`push`（押し込み）・`pull`（引き寄せ）・`followMove`（追従）・`poison`（毒）・`trapSet`（罠設置）・`musouRecover`（無双）。これらだけログ上で顔アップ画像（`images/chars/<charId>_face.png`）＋キャラ名＋tick＋「◯◯発動」の金色カード（`.log-skillcard`）として挿入される。それ以外（攻撃・ダメージ・撃破等）は今まで通り軽量な1行、**移動だけはさらに小さい点＋薄い文字（`.log-move`、opacity 0.55）に格下げ**した
  - **前提修正**: `push`/`pull`/`poison`のログイベントには元々「誰が発動したか」を示す`unitId`が無く`targetId`（効果を受けた側）しか記録されていなかった（`applyPush`/`applyPull`/`resolveAttack`の該当箇所で`attacker`はスコープ内にいたが記録し忘れていた）。カードに発動者の肖像を出すため、この3イベントに`unitId: attacker.id`を追加した
  - ダメージ/回復の数値も色分け（ダメージ`-N`赤、回復`+N`緑）、攻撃者・対象のキャラ名もチーム色（P1青/P2赤）で表示するよう`entityLabelHtml()`を新設（旧`entityLabel()`のテキスト版は未使用になったため削除）
  - 実機検証済み: ランサー2体vsトラッパー+ソルジャーの戦闘で、ランサーの「近接連携攻撃発動」カード・トラッパーの「罠設置発動」カードが肖像画付きで正しく表示され、間の移動イベントは小さい点表示に、攻撃/ダメージ行はチーム色付きの軽量テキストのままであることをDOM・スクリーンショット両方で確認。コンソールエラーなし
  - 追従・毒・引き寄せは引き続き現行キャラ構成では発動条件を満たすキャラがおらず未検証（星ランク制度が撤廃済みのため。コード自体は他の5種と全く同じパターンなので実装は共通）

- **[2026-09-06] バグ修正: 戦闘中にHPバーの黒い背景だけが配置位置に置き去りになる**。UC報告「キャラを配置した位置に移動しても薄っすら残ってる」「拠点のHPバーのとなりとかにキャラのHPバーだけなんか残ってる」。
  - **原因**: `createBattleMarker()`が生成するHPバーは黒い半透明の背景`rect`（`hpBg`）と色付き前景`rect`（`hpFill`）の2枚重ねだが、`battleMarkerEls`に保存するオブジェクトに`hpBg`を含め忘れていた。ユニット移動時にアニメーションで位置更新する`moveMarkerTo()`・最後まで飛ばした時に位置を直接セットする`skipToEnd()`は、どちらも保存済みの参照経由でしか要素を更新できないため、`hpFill`（前景）は正しく追従する一方`hpBg`（背景）だけが**生成時＝配置フェイズでの初期位置に永久に固定**されていた。前景バーがどれだけ動いても、その出発点に薄い黒バーが残り続ける見た目になっていた
  - **修正**: `battleMarkerEls.set()`に`hpBg`を追加し、`moveMarkerTo()`に`tweenAttr(m.hpBg, ...)`を、`skipToEnd()`に`m.hpBg.setAttribute(...)`をそれぞれ追加（`hpFill`と全く同じ位置指定）
  - 実機検証済み: 通常のtickごと再生・「最後まで」スキップの両方で、全ユニットの黒背景バーの`x`座標が前景バーおよび本体circleの座標と完全一致することをDOM直接確認。コンソールエラーなし

- **[2026-09-06] バランス調整第1弾: R2〜R4の基礎クレジットを引き下げ**。UC報告「少なくしたいんだよね、今めっちゃ買えるから」への対応。旧`ROUND_BASE_CREDITS = { 2: 500, 3: 400, 4: 600 }`は繰越（未使用クレジットが無制限に次ラウンドへ持ち越される）と合わさり、R2時点でほぼ全キャラを購入できてしまっていた。`{ 2: 200, 3: 200, 4: 300 }`に変更（R1の200crと同程度の感覚で、毎ラウンド新規2体前後を増強するペースを狙った値）。勝者ボーナス+200・繰越の仕組み自体は変更なし。設計書§7の表も更新済み
  - 実機検証済み: R1でソルジャー1体（残150cr）ずつ購入→引き分け決着→R2でクレジットが150+200=350になることを確認。コンソールエラーなし
  - **バランス調整は継続中のテーマ**。設計書§11「バランス調整メモ」に既知の懸念（クレリックの理論値、勝者ボーナスのスノーボール度合い等）が挙がっているので、今後もUCからの調整依頼が続く見込み

- **[2026-09-06] GitHub Pagesで公開（オンライン化）**。UC要望「OK　これでいったんオンライン化しよう」への対応。従来`index.html`はローカル（`python -m http.server`等）でしか開けず、Firebase Realtime Database自体はクラウド上で稼働していても、ゲーム画面自体を友達に共有する手段が無かった。
  - GitHub CLI (`gh`) をwinget経由でインストールし、UC本人に`gh auth login`（ブラウザでのデバイスコード認証）を実施してもらった（GitHubアカウント: Akisame4）
  - リポジトリ: https://github.com/Akisame4/auto-formation-battle-encounter （public。`gh repo create --source=. --push`で作成と同時に初回pushまで実施）
  - `.gitignore`にExcelのロックファイル（`~$*.xlsx`）を追加してコミット対象から除外
  - GitHub Pagesを`gh api -X POST repos/.../pages -f "source[branch]=main" -f "source[path]=/"`で有効化（mainブランチのルートを配信）
  - **公開URL: https://akisame4.github.io/auto-formation-battle-encounter/**（`index.html`が起点。相対パスの`images/chars/...`もそのまま機能する）
  - 実機検証済み: 公開URLでロビー画面が正しく表示されること、「ルームを作成」でFirebaseに接続し合言葉が発行されること（実際にRealtime Databaseへ書き込みできている）を確認
  - **今後の更新の反映方法**: `index.html`等を変更したら、このリポジトリで`git add`→`git commit`→`git push`するだけで、GitHub Pagesが自動的に再ビルド・再公開する（数十秒〜1分程度のビルド待ちが発生する場合がある）。gh CLIは`C:\Program Files\GitHub CLI\gh.exe`にインストール済み（セッション再起動後はPATH経由で`gh`とだけ打てば動くはずだが、Git Bash側でPATHが反映されない場合はフルパス指定が必要）

- **[2026-09-06] P2（赤陣営）のキャラカード背景を赤に修正**。UC要望「赤の陣地のプレイヤーの方のキャラの背景は赤にして」への対応。`.h-banner`（購入モーダル・配置待ち/配置済みカード上部の全身画像バナー）と`.h-icon`（指示パネルの小アイコン）が、所有者に関わらず常に`var(--p1)`（青）のグラデーション固定になっていたバグ（見落とし）を修正。
  - `.h-banner.p2, .h-icon.p2 { background: linear-gradient(160deg, var(--p2), var(--p2-dark)); }`をCSSに追加し、JS側の該当4箇所（`renderCharList`＝購入モーダル、`renderQueueList`の配置待ち/配置済みカード、`renderUnitEditPanel`の指示パネルアイコン）でowner（購入モーダル・配置待ちは`currentPlayer`、配置済み・指示パネルは`unit.owner`）が`"player2"`の時だけ`p2`クラスを付与するようにした
  - 盤面上のユニットマーカー（`battleColor()`使用）・拠点HPバー等は元々`owner`で正しく色分けされていたため対象外（今回のバグはカード/アイコンUIのみ）
  - 実機検証済み: 操作プレイヤーをP2に切り替えて購入モーダル・配置待ちカード・配置済みカード・指示パネルアイコンの4箇所全てが赤グラデーションになること、P1に戻すと青のままであることを確認。コンソールエラーなし（拡張機能由来の既知の無害なメッセージのみ）

- **[2026-09-06] 再配置の仕組みを完全撤廃**。UC判断「再配置いらないかも、、、ラウンドごとに戻せる仕様なしにしよう　これは完全に消しちゃっていいよ」への対応。従来、前ラウンドから引き継いだユニットはラウンドごとの上限体数（R2:2体/R3:3体/R4:4体）まで盤面上で位置を変更できたが、この仕組み自体を削除し、**前ラウンドから引き継いだユニットは配置フェイズ中も一切位置を変更できない**（前ラウンド終了時点の位置に完全固定）仕様に変更した。指示（目的）の変更は従来通り自由に行える。
  - `RELOCATE_LIMIT`定数・`relocatedThisRound`Set・`startRelocateUnit()`/`handleRelocateUnit()`関数・`mode==="relocateUnit"`分岐・指示パネルの「再配置する」ボタン群を全て削除。前ラウンド引き継ぎユニット選択時は「前ラウンドから引き継いだキャラは配置を変更できません。」という説明のみ表示するようにした
  - 今ラウンドで新たに購入・配置したユニット専用の`moveUnit`（上限なし移動）・`returnPlacedUnit`（返却）は元々別モード・別判定（`roundStartUnitIds`）だったため影響なし。そのまま存続
  - 設計書§7の表から「再配置上限」列を削除し、該当ルール文を「前ラウンドから引き継いだ既存キャラは位置を変更できない」に書き換え
  - 実機検証済み: R1にソルジャーを配置→両者「守る」で引き分け決着→R2へ進行→引き継いだソルジャーを選択すると再配置ボタンが無く上記の説明文のみ表示されること／同じR2で新規購入したソルジャーは従来通り「配置位置を変える」「返却する」ボタンが出ることを確認。コンソールエラーなし（拡張機能由来の既知の無害なメッセージのみ）

- **[2026-09-06] スキル発動時にスキル名をふわっと表示**。UC要望「スキルを発動したとき場面に発動した場所にふわっとスキル名を出してほしい」への対応。既存のダメージ数字と同じ`.dmg-float`アニメーション（`floatUp` 0.9s ease-out）を流用した`spawnSkillLabel(col, row, skillName)`を新設（ダメージ数字より一段上・小さめ・金色`#ffd479`で表示し重ならないようにした）。
  - 対象にしたのは「発動の瞬間」が明確なアクティブスキルのみ: `followMove`（追従）/`push`（押し込み）/`pull`（引き寄せ）は`applyEventsForTick`の共通move系caseに条件分岐で追加、`linkedAttack`（近接/遠距離連携攻撃）は専用caseを新設（従来はdefaultで何も表示していなかった）、`poison`（毒）と`trapSet`（罠設置）は既存caseにラベル呼び出しを追加
  - `linkedAttack`のログイベントには従来スキル名の情報が無かったため、`triggerLinkedAttacks()`のログ生成箇所に`skill: hasMelee ? "近接連携攻撃" : "遠距離連携攻撃"`を追加してどちらが発動したか判別できるようにした
  - 意図的に対象外としたもの: `musouRecover`（既に「無双!」を表示済みで対応不要）、`heal`系（既に+N表示があり、範囲回復で対象者ごとにスキル名が並ぶと煩雑になるため見送り）、発動の瞬間が無い常時系スキル（拠点破壊・強化・射撃N・射程ボーナスN・攻撃消費N・身軽・移動ボーナスN・範囲攻撃）
  - **現状のキャラ構成での留意点**: 追従・毒・引き寄せは、今シーズン内で撤廃済みの星ランク強化でのみ入手できたスキルのため、base 8キャラの構成では現在発動条件を満たすキャラが存在しない（コードは実装済みで、ランク制度が復活すれば動く）。実機で確認できたのは`push`（押し込み/ガーディアン）・`trapSet`（罠設置/トラッパー）・`linkedAttack`（近接連携攻撃/ランサー）の3種
  - 実機検証済み: ローカルテストモードで①ガーディアンvsソルジャーの戦闘で「押し込み」ラベルがダメージ数字と同時にDOM上に出現すること（アニメーション再生中に確認）②ランサー2体vsソルジャー1体で「連携攻撃」イベントがログに記録されること③トラッパーの移動のたびに「罠設置」イベントがログに記録されることを確認。連携攻撃・罠設置のラベル自体の描画はコードパス（`spawnSkillLabel`呼び出し）がpushと共通のため実装は同一だが、バックグラウンドタブでの実行速度低下によりアニメーション再生中のライブ確認は連携攻撃側では完了できず、ログレベルの発火確認とコードレビューでの整合性確認にとどめた

- **[2026-09-06] 配置画面の盤面まわりを3点強化**。UC要望「①残りHPが配置画面でも見えるように（削れたHPを次のラウンドで見てわかるように）②盤面のキャラを選択できるように（指示が出せるように）③（できれば）マウスドラッグで一括指令」への対応。
  - **①HPバー表示**: `appendMarkerHpBar(x, y, hp, maxHp)`を新設し、配置画面のユニット・拠点マーカー上にも戦闘画面と同じ見た目のHPバーを描くようにした（`render()`から呼び出し）。新規配置ユニット・R1の拠点は`hp`フィールドを持たないため満タン表示、前ラウンドから引き継いだユニット・拠点は`unit.hp`/`b.hp`の実値で削れて見える
  - **②盤面クリックでの選択が実は機能していなかったバグを修正**: `selectUnitForEdit()`自体は前から実装済みで正しく動いていたが、`render()`が描くユニット・拠点マーカーの`<circle>`要素に`pointer-events`指定が無く、既定でクリックを奪ってしまっていた。マーカー本体（画像部分）は既に`pointer-events:none`だったが、その下に重なる色付きの丸だけが対象漏れしており、**キャラの絵のど真ん中をクリックすると何も起きない**状態だった（外周のわずかな隙間をクリックした時だけ偶然ヒットしていた）。ユニット・拠点両方の`<circle>`に`pointer-events:none`を追加し、常に下のヘックスタイル（`hex-clickable`）にクリックが届くようにした
  - **③一括指令（ドラッグでコピー）**: 事前選択は不要で、盤面上の自分のユニットからマウスボタンを押したままドラッグし、他の自分のユニットを通過すると、ドラッグ起点ユニットの**現在の指示**をコピーする。「指定マスへ」は目標マスがユニット固有のためコピー対象外（起点にするとドラッグ自体が始まらない）。`startDragPaint`/`applyDragPaint`をヘックスタイルの`mousedown`/`mouseenter`に追加し、既存の`click`（選択・配置等）とは独立したイベント系統にした。ドラッグ終了（`mouseup`）時にコピーした体数を含むステータスメッセージを表示
  - **実装時に踏んだバグ**: ドラッグでコピーが成立すると`dragPaintMoved`フラグを立て、「ドラッグの終端で偶然発火するclickを1回だけ無視する」つもりだったが、**異なるヘックス間をまたぐドラッグ（＝mousedownとmouseupの対象要素が異なる）ではブラウザはそもそもclickイベントを発火しない**という仕様を見落としていた。そのため`dragPaintMoved`を消費してくれるclickが来ず、**フラグが立ちっぱなしになり以後の盤面クリックが全て無視される**という重大な回帰バグを実機テストで発見。`mouseup`ハンドラで`setTimeout(...,0)`により次のイベントループでリセットするよう修正（同一tick内で同期発火する「戻り先が同じマスだった場合のclick」の抑制は活かしつつ、クロスヘックスのドラッグ後もフラグが残らないようにした）
  - 実機検証済み: ①前ラウンドで被弾したユニット・拠点のHPバーが次ラウンドの配置画面で削れて表示されること②`elementFromPoint`でマーカー中心が正しく下のヘックスタイルを指すこと・実際のマウス座標クリックでキャラが選択できること③1体を「守る」に設定→ドラッグで別の2体に指示をコピー→ステータスメッセージ表示→ドラッグ後の通常クリックも問題なく機能すること、をすべてコンソールエラーなしで確認

- **[2026-09-05] 戦闘中UIの改善第2弾: 行動力切れユニットを減光**。UC要望「行動力が残っているキャラはハイライトをつけるか、行動力がなくなったキャラは少し暗くして見分けをつけれるようにして」への対応。ハイライト追加ではなく「行動力切れのユニットを`opacity:0.4`に減光する」方式を採用（相対的に行動力が残っているユニットは通常の明るさのままで自然に区別できるため、視覚的なノイズが少ない）。
  - `updateMarkerActivity(entity)`: 拠点・死亡ユニット（既存のdeath-fadeで別途表現）は対象外。`canStillAct = entity.ap > 0 || currentRound === 4`（R4はHP→行動力変換で行動力0でも生きていれば動けるため常に減光しない）で判定し、`m.g.style.opacity`を切り替える。`#gBattleUnits > g`に`transition: opacity 0.25s`を付けて滑らかに変化させる
  - **実装時に判明した既存の抜け**: P4のアニメーション再生層（`applyEventsForTick`）は、これまで`move`イベントで座標だけ更新し行動力(`entity.ap`)を一切減算しておらず、`attack`イベントに至ってはcaseが無く何もしていなかった（HPバー等はダメージイベント側で更新されるため気づかれなかった）。今回`move`に`apCost`減算を追加し、`attack`用のcaseを新設して`updateMarkerActivity`を呼ぶようにした。「最後まで」スキップ時は`simulateBattle()`が返す最終状態（`battleResult.units[].ap`が正しい値）をそのまま使うため元々問題なし
  - `restartPlayback()`（tick0・巻き戻し）でも全ユニットにこの判定を適用し、巻き戻すと減光がリセットされることを保証
  - 実機検証済み: 行動力6のトラッパー（rush、拠点破壊の同盟なし）が全行動力を使い切って`opacity:0.4`に減光される一方、行動力を使っていないガーディアン（guard）は通常の明るさのまま、という対比を確認。「最初」に巻き戻すと両方とも通常表示に戻ることも確認。コンソールエラーなし

- **[2026-09-05] 重要バグ修正: 次のラウンドへ進もうとすると戦闘画面の最初に戻され、ホスト/ゲストでラウンドがズレる不具合**。UC報告「（ゲスト側かな？）次のラウンド行こうとすると、もう一度戦闘画面の最初に戻されることがある。それでラウンド2と3にホストとゲストが分かれる」。
  - **原因**: `advanceToNextRound()`（および「再戦する」ボタン）が、Firebaseへの書き込みを`update(rooms/{code}, {status:"placing"})`と`update(rooms/{code}/placements/{myRole}, {ready:false})`の**2回の別々のupdate()**に分けていた。片方のクライアントが次のラウンドへ進む処理を実行した直後、この2つの書き込みは別トランザクションなので、もう片方のクライアントは一瞬だけ「statusは既にplacingに変わったが、相手のreadyはまだ前の戦闘の名残でtrueのまま」という**矛盾した中間状態**を観測できてしまう。ちょうどそのタイミングで自分（ホスト）側の`myReady`もまだ前の戦闘の名残でtrueのままだと、ホスト側の「両者ready検出→開戦」ロジック（`if (myRole==="player1" && myReady && opponentReady && data.status!=="battle")`）が誤発火し、**同じラウンドの戦闘がseedを取り直して勝手に再スタート**してしまう。両クライアントとも`!battleMode`で判定して新しい戦闘に入るため、片方だけこの「幻の戦闘」を最後まで見て「次のラウンドへ」をもう一度押すと、そちらのクライアントだけラウンドが余分に1つ進む（前回修正した`roundOutcome._advanced`ガードは「同じ戦闘結果を2回進める」事故を防ぐものだったが、今回は**新しい戦闘そのものが誤って生成される**ケースだったため別問題として再発していた）
  - **修正**: `advanceToNextRound()`・rematchBtnハンドラの両方で、`status`と`ready`の書き込みを**1回の`update()`にまとめてアトミックに反映**するよう変更（`update(ref(db, rooms/{code}), { status: "placing", [\`placements/${myRole}/ready\`]: false })`という複数パス同時更新のFirebaseの標準手法）。これにより中間状態が一切観測できなくなり、上記の誤発火が起きなくなる
  - 実機検証済み（2タブでホスト作成→ゲスト参加→両者配置・準備完了→自動開戦→両者「最後まで」→**ゲスト側を先に**「次のラウンドへ」→ホスト側が戦闘画面のまま巻き戻らないことを確認→ホスト側も「次のラウンドへ」→両者ともラウンド2・クレジット640で一致することを確認）。コンソールエラーなし

- **[2026-09-05] 戦闘中UIの改善第1弾: 罠マークを追加**。UC要望「戦闘中のUIを工夫しよう、まずトラッパーが動いて罠を設置した場所には罠マークをつけてわかるようにしよう」への対応。従来、罠は`state.traps`/`persistentTraps`という**データとしてのみ**存在し、盤面上にまったく可視化されていなかった（設計書§8スキル#6は「相手からも見える」と規定しているのに、実装は完全に不可視だった）。
  - **戦闘シミュレータ側の変更**: `performAction()`の罠設置処理（`char.skills.includes("罠設置")`）に、従来無かった`{type:"trapSet", unitId, col, row, owner}`ログイベントを追加。旧実装は罠設置がログに一切残らず（CLAUDE.md内P6セクションの「罠は即時終了tick時点での正確な再構築ができない」という既知の簡略化の原因でもあった、そちらは今回は未修正のまま）
  - **表示**: 新しいSVG層`#gTraps`（`#gAttackRange`と`#gMarkers`の間、ユニットの下・盤面の上）に、罠のあるマスへ所有者カラー（P1青/P2赤）の菱形＋「罠」ラベルを描画する`addTrapMarker(col,row,owner)`。同じマスへの重複描画は`renderedTrapKeys`のSetで防止
  - **表示箇所**: ①配置フェイズ（`render()`内で`seedTrapMarkersFromPersistent()`を呼び、前ラウンドから引き継いだ罠を常時表示。「相手からも見える」仕様に対応）②戦闘アニメーション再生中（`applyEventsForTick()`が`trapSet`イベントで逐次追加）③「最後まで」スキップ時（`battleResult.traps`の最終状態から直接一括描画）④「最初」に巻き戻し・新しい戦闘への突入時（`restartPlayback()`が`persistentTraps`だけに引き直すので、今回の戦闘で新設された罠は巻き戻すと消える＝tick0時点の状態と整合）
  - 実機検証済み: トラッパーを配置してrush移動させ「最後まで」再生→移動経路上に罠マークが複数表示・ログに「罠設置」記録／通常のラウンド決着で「次のラウンドへ」進んだ後もR2の配置画面に罠マークが引き継がれて表示されること、をコンソールエラーなしで確認

- **[2026-09-05] 重要バグ修正: 2クライアント間でラウンド・クレジットがズレる不具合**。UC報告「二人の対戦画面がラウンドが違くなっている、クレジットも違う」（スクリーンショット2枚で片方が「R2/クレジット725」、もう片方が「R3/クレジット905」になっているのを提示）。テスト環境は1台のPCで2タブを開き一人でP1/P2を交互操作。
  - **原因**: `advanceToNextRound()`に多重実行防止のガードが無く、「次のラウンドへ」ボタンの2度押し（誤操作・イベントの重複発火等）が起きると、**1クライアントだけ**同じ戦闘結果に対してラウンド進行処理が2回走ってしまう。2回目の実行時点で`currentRound`は既に1つ進んでいるため、`nextRound = currentRound + 1`により**ラウンドが余分にもう1つ進み**、`ROUND_BASE_CREDITS`のクレジットや勝者ボーナスも**二重に加算**される。もう片方のクライアントは1回しか押していないため、両者の状態が食い違う（オンライン同期自体は正常で、ズレの原因はローカルの多重実行だった）
  - **修正**: `roundOutcome`オブジェクトに`_advanced`フラグを立て、`advanceToNextRound()`の先頭で`if (!outcome || outcome._advanced) return;`により同じ戦闘結果に対しては1回しか実行できないようにした。新しい戦闘が終わるたびに`roundOutcome`は新しいオブジェクトに差し替わるため、次のラウンドでは正常に動作する
  - 実機検証済み: `advanceToNextRound()`相当のボタンクリックを意図的に2連続実行し、1回目でラウンド1→2・クレジット反映、2回目はラウンド・クレジットとも変化しない（無視される）ことを確認。コンソールエラーなし

- **[2026-09-05] 試合終了ポップアップ（勝利/敗北/引き分け）＋再戦・ホームに戻るボタンを追加**。UC要望「ゲーム勝利したら、勝利者には勝利のポップアップ、敗北者には敗北のポップアップを再生終了後にながす。その画面にも再戦か、ホームに戻るボタンを作る」への対応。
  - `showBattleResult()`が計算する`isMatchOver`（全滅・拠点陥落・4R決着＝設計書§6-1の即時終了条件）が真の時のみ、`showMatchResultPopup(winner, reasonLabel)`を呼んで新しい`#matchResultScrim`モーダルを開く。ラウンド決着（`roundEnd`、次のラウンドへ進む通常ケース）では出さない
  - **視点判定**: オンライン中は`myRole`、オフライン単体テスト中は`currentPlayer`（デバッグ用の操作プレイヤー切替）を「自分」とみなし、`roundOutcome.winner`と比較して「勝利」「敗北」「引き分け」を出し分ける（`.match-result-modal`に`win`/`lose`/`draw`のクラスを付けてCSSで色分け。勝利=ゴールドの発光、敗北=赤系、引き分け=グレー）
  - モーダルは背景クリック・Escでは閉じない（「再戦する」「ホームに戻る」どちらかを選ばせる終端画面という位置づけ）
  - `resetGameState()`という共通関数に切り出し（旧`resetBtn`ハンドラの中身そのまま）、「全てリセット」ボタンと今回の「再戦する」ボタンの両方から呼ぶようにした
  - **再戦する**: `resetGameState()`でR1初期状態に戻し配置フェイズへ。オンライン中は`room.status`を`"placing"`に戻し自分の`ready`をfalseにする（P6の「次のラウンドへ」と同じ、両者が独立に同じ操作をして決定論的に収束する設計を踏襲。ホスト/ゲストどちらが先に押しても・両方が押しても壊れない）
  - **ホームに戻る**: オンライン中は自分のオンラインフラグをfalseにしてから`resetGameState()`→既存の`leaveRoomLocal()`（購読解除・ルーム離脱・ロビー画面Aへ）を呼ぶ。オフライン中はそのままロビーへ
  - 実機検証済み: P1視点で全滅勝利→「勝利」ポップアップ（ゴールド）→「再戦する」でR1・クレジット200にリセットされ配置画面に戻ることを確認。同じ結果をP2視点（デバッグの操作プレイヤー切替）で見ると「敗北」（赤）になることを確認。「ホームに戻る」でロビー画面に戻ることを確認。コンソールエラーなし

- **[2026-09-05] 強化（★2/★3・ランク）の概念を一旦撤廃**。UC判断「キャラ合成微妙な気がしてきた、いったんなしにランクの概念を消そう。もしかしたらありに変更するかもしれないから、メモには作り方だけ残しておいてほしい」への対応。
  - **現状**: 全キャラは常に基礎ステータス（設計書§9の表）のみ。`getCharacter(charId)`は単に`getBaseCharacter(charId)`を返すだけの関数に戻した
  - **削除したコード**: `CHAR_UPGRADES`テーブル・`getEffectiveCharacter()`・`MAX_STAR`・ユニットの`star`プロパティ・合成関連一式（`findMergeCandidate()`/`startMergeUnit()`/`handleMergeUnit()`/`mode==="mergeUnit"`分岐/指示パネルの合成ボタン・合成待ち専用パネル/盤面の合成候補ハイライト`.merge-candidate-ring`とそのCSSアニメーション）。カード名の`★N`表示、Firebase同期の`purchaseCount`（こちらは前回セッションで既に削除済み）も無し
  - **「作り方」の記録**: 過去に試した2方式の実装詳細は消さずに残してある。①同キャラ購入で全ユニットが自動強化する旧方式→本ファイルの「P6: ラウンド進行」セクション内`**強化★2/★3（...後に撤廃...）**`の項。②盤面上で同キャラ・同星2体を手動合成する方式→本セクション内`**[2026-09-05実装→2026-09-05に撤廃]...**`の項（UIの分かりやすさを改善した追記も含む）。設計書§9「強化内容」の数値表（★2/★3の具体的な強化値）もそのまま残してある。**再検討する際はこれらの記録を読んでから作り直すこと（ゼロから設計し直さない）**
  - 実機検証済み: キャラ2体購入→両方配置→指示パネルに合成関連の表示が一切出ないこと、名前に★が付かないこと、戦闘が正常に完走すること、をコンソールエラーなしで確認

- **[2026-09-05実装→2026-09-05に撤廃] 強化（★2/★3）を「同キャラ購入で自動強化」→「盤面上での手動合成」に変更した記録**（UC判断で結局しっくり来ず、後述の通り強化システム自体を撤廃した。**再検討時の実装参考としてこの節を保持**。設計書は現在このセクションを反映していない点に注意）。UC要望「同じキャラを複数体購入するだけでランクが上がるのではなく、キャラ同士を合成することでランクがあがる仕組みにしたい」への対応として作った。方式決定はチャットで相談しながら詰めた: 合成できる場所は盤面上のユニット同士のみ（配置待ちキューでは不可）／操作は自動ではなく手動（自動だと同じキャラを複数並べて置く戦略ができなくなるため却下）／合成後は選んだ側のHP・行動力を維持／合成のクレジット追加コストは無料。
  - **データモデルの変更が本質**: 星は「プレイヤー×キャラごとの購入累計」（`purchaseCount`＋`starLevelOf()`）ではなく**ユニットごとの`star`プロパティ**（既定1、`MAX_STAR=3`）になった。`purchaseCount`とその全ての参照（購入時の加算、返却時の減算、リセット処理、Firebase同期）を完全に削除——星情報はユニット自体に乗るため、既存の`units`同期だけで相手にも正しく伝わり、専用の同期コードは不要になった
  - `getCharacter(charId, owner)` → `getCharacter(charId, star)`にシグネチャ変更（第2引数の意味がプレイヤーから星に変わった）。呼び出し元は「星を持つ対象（盤面ユニット・戦闘状態ユニット）は`.star`を渡す」「星を持たない対象（配置待ちキューの購入前キャラ・ショップのカタログ表示）は引数省略で★1扱い」に整理。`initBattleState()`が戦闘状態ユニットにも`star`をコピーするようにし、戦闘シミュレータ内部の全呼び出し（`isBlockedForUnit`/`attackRangeCells`/`effectiveAtk`/`resolveAttack`/`triggerLinkedAttacks`/`triggerHealAura`/`triggerFollow`/`performAction`/`computeCarryover`）を`.owner`→`.star`に更新
  - 合成のUI/ロジック: `findMergeCandidate(unit)`（盤面上で同owner・同charId・同starの別ユニットを検索）→ 指示編集パネルに「合成する（★N+1にする）」ボタン（候補が無ければ「もう1体あれば合成できます」ヒント、★3なら「既に★3（最大）です」ヒント）→ `startMergeUnit()`で新モード`mode="mergeUnit"`に入り盤面クリック待ち→ `handleMergeUnit(col,row)`が`unitAt()`で相手ユニットを取得しowner/charId/star一致を検証→ 相手をunits配列から削除し、選択していた側の`star`を+1（HP・行動力はそのまま）。既存の返却・再配置・移動の対象判定（`roundStartUnitIds`）とは独立しており、前ラウンド引き継ぎユニット・今ラウンド新規ユニットのどちらも合成対象にできる
  - `CHAR_UPGRADES`テーブル自体（★2/★3の差分値定義）は変更なしで流用。ショップカードの「購入で★Nに強化」アップセル表示は仕組みごと廃止（もう自動強化ではないため）
  - 実機検証済み: ガーディアン2体購入→両方盤面に配置（自動合体しないことを確認）→片方を選択して「合成する」ボタン表示→合成実行→1体消えてHP350（★2の実効値）に反映→合成後に戦闘を実行し正常に完走、をコンソールエラーなしで確認
  - **[2026-09-05追記] UC報告「合成の仕方が分らない（ボタンを押した後、次に何をすればいいか分からない）」への対応**。合成待ち中（`mode==="mergeUnit"`）の間、①`renderUnitEditPanel()`が通常の指示チップ等を含む本来のパネルではなく、「合成相手を選んでください」＋ヒント＋「キャンセル」ボタンだけのシンプルな専用表示に切り替わる、②`render()`のユニットマーカー描画で、合成候補（同owner・同charId・同star）の盤面マーカーに緑の点線リングを付け`.merge-candidate-ring`のCSSアニメーション（`mergePulse`、1秒周期で明滅）で光らせる、の2点を追加した。キャンセルボタンは`mode="idle"`に戻すだけのシンプルな実装。実機検証済み（合成候補が盤面で光ること、光っているユニットをクリックすると合成が成立すること、キャンセルで両方とも★1のまま盤面に残ることを確認。コンソールエラーなし）

- **[2026-09-05] 重要バグ修正: 全滅・拠点陥落したのに次のラウンドへ進めてしまう不具合を修正**。UC報告「拠点を落とされているのに次のラウンドにいけてしまっている」で発覚。設計書§6-1「全滅・拠点陥落は即時終了条件＝試合そのものの終了（残り行動力・残りラウンドは処理しない）」に反し、`advanceToNextRound()`が`currentRound >= 4`しかチェックしておらず、1〜3Rで全滅・拠点陥落が起きても`roundOutcome.endReason`（`interpretRoundOutcome()`が正しく`"annihilation"`/`"baseFall"`を算出していた）を一切見ていなかったため、常に次ラウンドへ進めてしまっていた。
  - `advanceToNextRound()`の先頭ガードを`currentRound >= 4`→`outcome.endReason !== "roundEnd" || currentRound >= 4`に変更（§6-3により4Rは必ずこの条件に該当するので、実質的に統合された一本のチェックになった）
  - `showBattleResult()`にも同条件で`isMatchOver`判定を追加し、試合が終わっている場合は「次のラウンドへ」ボタン（`roundAdvancePanel`）自体を表示せず、ラベルも「ラウンド結果」→「試合結果」に変更、`showStatus()`で「試合終了。最終結果: ○○」を即座に表示するようにした（旧実装はボタンを消さず、ユーザーが気づかず押せてしまっていた）
  - 実機検証済み: P2のキャラを0体のまま開戦→全滅判定でR1終了時に「次のラウンドへ」が出ず「試合終了」表示になること、`nextRoundBtn`を直接クリックしてもラウンドが進まないこと、通常の行動力切れによるラウンド決着（`roundEnd`）では従来通り「次のラウンドへ」が出てR2に進めること、をコンソールエラーなしで確認

- **[2026-09-05] 戦闘フェイズの終了条件を厳密化・50tick上限を廃止**（設計書§5-1更新済み）。UC要望「全員が行動力0になるか待機状態になる、つまりこれ以上盤面が動くことがない時はすぐ終了になるようにしよう。あとtick上限で終了をなくす」への対応。
  - `simulateBattle()`のwhileループから`maxTick`（50）を撤廃し無条件ループに変更。代わりに、そのtickで実際に処理した全ユニットの行動が「stay」（動けず停止・行動力消費なし）のみだった場合＝盤面が一切変化しなかった場合に`tickHadRealAction`フラグで検出し、その時点で即終了するようにした（旧実装は行動力0=`canAct`falseでしか終了せず、全員行動力が残っていても手詰まりなら50tickまで無駄にtickを消化していた）
  - 終了は必ず有限時間内に収まる（無双の行動力+3回復・R4のHP→AP変換も元手のHP/行動力自体が有限なため、いずれ全員行動力0か手詰まりに到達する）
  - `battleResult.endReason`は常に`"exhausted"`になった（`"tickLimit"`は削除）。`updateTickLabel()`の表示も「行動力枯渇/停止」に一本化
  - 実機検証済み: P1・P2とも「守る」で接触しない配置→**tick1で即終了**（旧実装なら50tickまで空回りしていたケース）。P1が単独で敵拠点を攻略する通常シナリオ→行動力が尽きるtick（20）まで正常に進行し早期終了しないこと、をコンソールエラーなしで確認
- **[2026-09-05] 仕様変更: 「この場を守る」の攻撃トリガーを「被弾時のみ反撃」→「範囲内に敵が入ったら通常攻撃」に変更**（設計書§4・§5-4更新済み）。UC要望「攻撃を受けた時のみではなく、射程に入ったら攻撃。ただしそのtickは行動しているキャラより優先度を低く。追加攻撃ではなく普通の攻撃判定で、連携や押し込みなども機能するように」への対応。
  - `simulateBattle()`のtickループを2段階シャッフルに変更: 「守る以外の全キャラ（ランダム順）」→「守るキャラ（この中もランダム順）」の順で行動させる（`normalIds`/`guardIds`）。守るキャラは同じtick内で他キャラより後に判定されるため、直前に他キャラが移動して射程に入った・敵を倒した等の変化を反映した状態で行動できる
  - 攻撃自体は`performAction`の通常の攻撃判定パス（`isExtra`フラグなし）にそのまま乗せることで実現。旧`checkCounter()`（被弾時に割り込みで`{isExtra:true}`の追加攻撃を行っていた関数）は完全に廃止し、連携攻撃・押し込み/引き寄せ・毒の新規付与もすべて通常通り発動するようになった
  - 副次的な変化: 旧実装では反撃コストが`攻撃消費N`スキルや罠の行動力加算を無視した固定1消費だったが、統一後は通常の攻撃と同じコスト計算になった（より一貫性のある挙動。仕様書に明記のない旧実装の簡易化だったため問題なし）
  - イベントログの`counter`種別は廃止（EVENT_LABELS・`applyEventHeadless`から削除）。すべて`attack`として記録される
  - 実機検証済み: P1(chase)とP2(guard)のソルジャー同士を対面させて`simulateBattle()`をブラウザで実行。ログで「t13: P1が移動して隣接→同tick内でP2(守る)が即座に攻撃」「t14以降: P1が先に攻撃→同tick内でP2が反撃的に攻撃」「t17: P2の攻撃でP1撃破」という2段階優先度の狙い通りの挙動を確認。ログ中に`反撃`の文字列が一切出ないことも確認（コンソールエラーなし）
- P1〜P5完了（実機検証済み）。P6はR1→R2の遷移まで実機検証済み（勝敗判定2パターン・強化★2・再配置とも確認）
- **[2026-08-22] P7: HP→行動力変換（§6-3）を実装**。R3→R4のブロックを解除、R4終了時は試合終了メッセージに変更。テストモード・バランス調整は未着手
- **[2026-08-19] §7ラウンド進行表を確定**（設計書§10未確定#3クローズ）。クレジット・制限時間・プール体数目標がR1〜R4すべて確定。R1のランダムプール抽選（8体中6体、オフライン・オンライン両方）・R2→R3のクレジット進行まで実装し実機検証済み（R1→R2→R3のクレジット計算、R3→R4のブロック、Firebase経由のプール同期を確認）。R2以降のプール**構成**（どのキャラが増えるか）は引き続き§10未確定#4
- **[2026-08-16] 仕様変更: 拠点を固定位置化**。手動設置UIを廃止し`FIXED_BASES`で自動配置に変更済み（実機検証済み）。設計書§2・§7も更新済み
- **[2026-08-17] 壁マスの正式レイアウト確定**。§10-1はクローズ済み。設計書§2・`WALLS_HALF`とも更新済み・実機検証済み
- **[2026-08-19] 押し込み/引き寄せの方向裁定を確定**（§10-2クローズ）。既存P3実装（最も角度が近い方向、タイは配列順）をそのまま正式仕様として採用。コード変更なし、設計書§8に明記
- **[2026-08-25] キャラ画像対応**。UC要望「キャラクターに画像を入れてわかりやすくしたい」に対応。UC側で`images/chars/<charId>.png`（例: `soldier.png`）を用意する運用（`images/chars/README.md`に説明あり）。ショップカード・配置キュー/配置済みカード・ユニット詳細パネルの`.h-icon`にはHTMLの`<img>`（`charImageTag()`、`onerror`で失敗時に非表示→既存の一文字アイコンにフォールバック）、盤面マーカー（配置画面・戦闘画面共通）にはSVGの`<clipPath>`+`<image>`（`appendCharMarkerImage()`、`error`イベントで同様にフォールバック）を追加。戦闘中の移動tweenにも追従させた（`moveMarkerTo()`/`skipToEnd()`を拡張）。画像未配置でも壊れない設計（読み込み失敗時は何もしなかったことになり、既存の色付き丸/文字表示がそのまま見える）。
  - **ハマったバグ**: `.h-icon`に`position:absolute`の画像を重ねるため`position:relative`を追加したが、既存CSSが`.hcard .h-icon`という祖先依存のセレクタで、`.inspector-who`配下の`.h-icon`（`.hcard`の外）にはこの祖先依存ルールが一切効いていなかった（サイズ上書きの別セレクタしか当たっていなかった）。結果、ユニット選択時にpositionの基準祖先が無い画像が画面全体を覆う致命的な表示崩れが発生。**教訓: 新しいCSSプロパティを追加する時は、対象セレクタが本当に全ての利用箇所（祖先要素の有無を含め）をカバーしているかgrepで確認する**。`.hcard .h-icon`→祖先非依存の`.h-icon`に変更して解消・実機検証済み
- **[2026-08-24] 攻撃範囲オーバーレイを追加**。UC要望「配置したユニットの攻撃範囲を表示して、戦闘中も表示してほしい」に対応。既存の戦闘ロジック用純粋関数`attackRangeCells(unit)`（射撃N=直線・射程ボーナスN=全方位・通常1マス、いずれもロジック側と完全に一致）をそのまま流用し、盤面に新しいSVGレイヤー`#gAttackRange`を追加。各ユニットの範囲マスを陣営カラーの破線枠で表示（既存の「自陣」塗りつぶしと視覚的に区別するためあえて塗らず枠のみ）。配置画面は`render()`内で常時再描画、戦闘画面は`restartPlayback()`（tick0）・`stepForward()`（毎tick）・`skipToEnd()`（最終tick）それぞれで`displayState.units`を渡して呼び直し、移動・死亡に追従させる。実機検証済み（射撃N持ち＝直線範囲、通常キャラ＝周囲1マスの六角リングが向きに応じて正しく表示、戦闘中に移動しても範囲が追従することを確認。コンソールエラーなし）
- **[2026-08-23] マップを20列×19行に縮小**。UC指摘「マップがデカくて見づらい」を受け、旧30行からrow7-9・13-17・20-22を削除し0〜18に振り直し（ROWS=19）。列見出しをアルファベット表記（A〜T）に変更。壁(`WALLS_HALF`)・拠点(`FIXED_BASES`のP2側row29→row18)も比率を保って再設計。実機検証済み（配置→戦闘の一連の流れ、盤面380マス描画、コンソールエラーなし）。設計書§2も更新済み
- **[2026-08-23] 戦闘バグ「ユニットidの衝突」を修正**（重要）。UCから「戦闘中の挙動がおかしい（1tick目で敵陣にワープ・異常な速さ・ホスト側が一切動かない・味方で戦闘してる）」と報告。最初は`entityLabel()`の表示不備（拠点は`拠点(P1)`と表示するのにキャラは名前だけで陣営不明）だと誤診断し、それだけ直して「バグではなく表示問題」と説明したが、**UCがスクリーンショットを提示して指摘し、再調査で真因が別にあると判明**。
  - **真因**: `unitSeq`（ユニットid採番カウンタ）がクライアントごとのローカル変数で、ホストとゲストがそれぞれ自分のタブで独立に1から数える。両者が同じ体数を配置すると、ホストの1体目とゲストの1体目が同じ数値id（例:1）を持つ。`startBattleFromRoom()`でP1・P2のユニットを1つの配列にマージして戦闘を始めると、id衝突したユニット同士が表示側のMap（`battleMarkerEls`: id→マーカー要素、`findDisplayEntity()`の`.find()`）で同じスロット/検索結果を取り合い、片方のマーカーが両陣営の移動イベントを同時に受けて盤面を暴れ回り（「瞬間移動」「異常な速さ」）、もう片方は更新されず固まる（「動かない」）。ログの`entityLabel()`も`.find()`で先頭一致するユニットに解決されるため、常に同じ陣営表示になる（「味方で戦闘」に見える）。**マップ縮小とは無関係の、オンライン対戦（ホスト+ゲストの実クライアント合流）でのみ顕在化する既存バグ**（1クライアントでのdev-modeテストは`unitSeq`が1本しかないため再現しない。ここが自分の再現テストで長時間見抜けなかった理由）
  - **修正**: `handlePlaceUnit()`でid生成を`${owner}_${連番}`に変更（例: `player1_3`）。owner文字列を含めることで構造的に衝突不可能にした。`unitSeq`の巻き戻り防止用`Math.max(...)`2箇所（`advanceToNextRound()`・`restoreMyPlacement()`）は文字列idだと`Math.max`がNaNになり壊れるため、`seqPartOfUnitId()`ヘルパーで数値部分だけ抽出するよう修正。他の`.id`比較箇所（`===`・`Set.has()`)は型に依存しないためそのまま動作
  - 実機検証: 2タブで実際にホスト作成→ゲスト参加→3体ずつ配置→開戦を再現（修正前にUCが遭遇したのと全く同じ条件）。修正前は本来ここでid衝突するはずの構成。修正後はtick2時点で両陣営が自陣内に留まり、ログが`ランサー(P1)`/`スナイパー(P2)`のように正しく陣営を区別、最後まで正常に戦闘が進行することを確認
  - **教訓**: ①UCの「おかしい」報告は再現できなくても軽視しない。②**再現テストが「同一クライアント内」に閉じていないか要注意** — 今回は2タブ実戦のつもりで何度も再現したが、実際に真因を突き止められたのはUCが送ってくれたスクリーンショット（盤面の実際の様子）を見てから。テキストログだけでなく実際の画面も早めに見せてもらうべきだった
- **[2026-09-02] キャラアイコン拡大（ポートレート型カードに刷新）**。UC指摘「画像が小さくてみえない」に対応。Artifactでモックアップ3案（アイコン拡大／ポートレート型／横長リスト）をUC確認 → **案B（ポートレート型）** を採用。
  - ショップ/配置待ち/配置済みカード（`.hcard`）: 旧`.h-icon`（24px角・名前と横並び）を廃止し、`.h-banner`（カード上部いっぱいの128px高バナー）に刷新。価格`.h-price`・未配置バッジ`.h-badge`はバナー右上に半透明の背景付きで重ねる形に変更（`renderShop()`・`renderQueueList()`のカードHTML生成を修正）
  - 指示パネル（`.inspector-who .h-icon`）: 30px→52pxに拡大（構成は変更なし、丸アイコンのまま）
  - 盤面マーカー（配置画面・戦闘画面共通）: ユニットの半径を拠点と同じ`HEX_SIZE*0.62`に統一（旧: ユニット0.52/拠点0.62で差があった）。`appendCharMarkerImage()`の呼び出し箇所すべてに反映
  - 実機検証はPlaywright（`chromium`）をローカルサーバ経由で自動操作し確認。ショップ/配置待ち/配置済みカードの画像表示・サイズ、指示パネル（過去にpositionの祖先依存バグで画面崩壊した箇所）のレイアウト、盤面・戦闘マーカーの画像表示、コンソールエラー0件を確認済み
- **[2026-09-03] 盤面マーカーに顔アップ画像を追加**。UC指摘「盤面の中はやっぱり見づらい」に対応。全身の立ち絵（`<charId>.png`）を小さい丸にクリップすると何のキャラか分かりづらいため、盤面マーカー専用に`<charId>_face.png`（顔アップ）を用意する運用を追加（`charFaceSrc()`、`images/chars/README.md`に追記）。カード側（`.h-banner`）は従来どおり全身画像のまま。
  - `appendCharMarkerImage()`を拡張し、顔画像→（読み込み失敗時）全身画像→（それも失敗時）色付き丸＋文字ラベルの3段フォールバックに変更
  - UCが7キャラ分`<charId>_face.png`を用意済み（ソルジャーのみ`soldier_cat.png`という誤ったファイル名になっており未反映だったが、**[2026-09-05] `soldier_face.png`にリネーム済み**。README注記も削除し8キャラ全員の顔アップ画像が反映済み）
  - 実機検証（Playwright）: 顔画像ありキャラ→マーカーに顔アップ反映、顔画像なし（当時のソルジャー）→全身画像に正しくフォールバックし表示が欠けないことを確認。配置画面・戦闘画面の両方のマーカーで確認済み。**[2026-09-05]** ソルジャーのリネーム後も実機（Chrome）で顔アップ画像の反映を確認済み
- **[2026-09-19] キャラ9体・スキル5種を追加＋既存4体のバランス調整**。UCが`AFBElist.xlsx`（スキル一覧シート＋Sheet1のキャラ一覧）を用意し「少しバランス調整したものと追加でキャラとスキルを用意した」と依頼。シートの「採用」欄1〜17番のうち既存8体分を除く9体（毒使い・門番・足軽・衛生兵・孤高・狩人・吟遊詩人・スカウト・王者）を追加。「まだ検討中」の13体（柴犬侍・ビーバー等）はスキル効果が未定義（マスタースキル表に無い独自スキル名のみ）のため今回は見送り。
  - **バランス調整**（既存4体、シートの数値通り）: ガーディアン行動力15→13／ランサー行動力18→16／アーチャー攻撃消費2→1／スナイパー射撃5→8・攻撃消費3→2。オーガはid・数値・スキルそのまま、表示名のみ「バーサーカー」に変更（見た目の熊差し替えと合わせてUC確認済み）
  - **新規スキル5種**（設計書§8の18〜22番として追加）: 縄張り（自陣で攻撃力2倍）・単体回復N（範囲内の味方1体をHP回復、攻撃より優先）・孤高（3マス以内に味方不在で攻撃力+20）・行進曲（自身が行動力3消費毎に範囲内の味方の行動力+1、1ラウンド5回まで）・血の匂い（指示を無視し最も近い敵を追う）。実装は`effectiveAtk()`（縄張り・孤高）／`performAction()`冒頭の優先回復チェック（単体回復N）／`triggerMarch()`新設（行進曲、`triggerHealAura`と同じapSpentTotal差分検出方式を流用）／`computeGoal()`冒頭（血の匂い）
  - **スキル名の表記ゆれ2件をUCに確認して解決**: シートのウシ「治療」→マスタースキル表に該当スキルが無く効果が一致する「単体回復20」として実装／シートのオオカミのスキル欄「一匹狼」（役職名は「孤高」）→マスタースキル表の「孤高」として実装。いずれもUC承認済み
  - 画像は前回セッションで整理済みの`game_characters_transparent_1`/`game_characters_faces`フォルダに新規9体分もUCが用意済みで、`CHAR_IMAGE_FILE_OVERRIDE`/`CHAR_FACE_FILE_OVERRIDE`（charIdとファイル名が一致しないキャラの個別対応マップ、trapper/ogre追加時に新設したもの）に9体分追記して対応。**毒使いのみ顔アップ画像（`snake_poisoner`相当）がfacesフォルダに見当たらず未対応**（全身画像は正常。盤面マーカーは色付き丸アイコンにフォールバックする。UCが用意可能なら追記対応）
  - `DECK_SIZE = Math.min(12, CHARACTERS.length)`は据え置き（キャラ総数17でも12のまま）。デッキ編成画面・R2以降のプール表示は`CHARACTERS`配列を動的に参照する実装のため、コード変更なしで17体が自動的に反映されることを確認
  - **検証方法**: ブラウザ操作が前回セッションでCDP経由のスクリーンショット縮小バグに度々遭遇していたため、今回は戦闘ロジック（`simulateBattle`ほか、DOM非依存の純粋関数群）をNode.js上に切り出したテストハーネス（firebase importをダミー関数に置換、`document`/`window`を最小スタブ化）を作り、5つの新スキルそれぞれについて期待値付きの単体テストを実施（縄張り: 自陣内外でダメージ30/15、単体回復20: 対象への回復量と回復優先で攻撃しないこと、孤高: 単独時+20の有無、行進曲: 3AP毎のトリガーと5回キャップ、血の匂い: guard指示でも移動すること）。全11アサーション成功。既存8体のバランス変更値・オーガ改名もNode側で数値確認。あわせてブラウザ（Chrome拡張、ローカルサーバ配信）でコンソールエラー0件・デッキ編成画面で17体全員の名前と立ち絵表示を確認済み
- **[2026-09-19] 配置完了キャンセル・拠点HP表示の分割・全軍突撃ボタン**。3件のUC要望に対応。
  - **配置完了キャンセル**: 「配置完了（準備OK）」の隣に「キャンセル」ボタンを追加。押すとFirebase側の`placements/{myRole}/ready`をfalseに戻し、まだ`status`が`"battle"`に切り替わっていなければ配置をやり直せる（両者ready検出はホスト側の非同期処理のため、僅差のタイミングでキャンセルが間に合わず開戦してしまう可能性はUC了承の上で許容）。2タブ実機テストでキャンセル→ボタン状態が戻ることと、その後改めて両者readyで正常に開戦することを確認済み
  - **拠点HP表示を2本に分割**: 従来は自軍拠点2個の合計HPを1本のバー（600/600）で表示していたが、UC要望「300、300で分けよう」を受けて`renderBaseHp()`を拠点ごとに1本ずつ描画するよう変更（P1-1/P1-2/P2-1/P2-2の計4本、各300/300）。実機確認済み
  - **全軍突撃ボタン**（前回セッションの続き）: 一度「4ラウンド目は指示無し（守るのまま）なら拠点へ自動で向かう」仕様を実装したが、UCから「それやっぱなしで」と撤回依頼があり、代わりに全ラウンドで使える「🚩 全軍突撃」ボタン方式に変更した（詳細は設計書§4）
- **[2026-09-19] SE（効果音）を6種追加**。UC要望「購入・配置・攻撃・スキル発動・指示のドラッグ・勝敗決定にそれぞれSEを」。外部音源ファイルは用意せず、Web Audio API（オシレータ+ゲインのエンベロープ、攻撃音のみホワイトノイズ併用）でその場合成する`playSE(name)`関数を新設（`SE_PRESETS`にbuy/place/attack/skill/drag/win/lose/drawの8プリセット）。フックした箇所: `buyCharacter()`（購入）／`handlePlaceUnit()`（配置）／`applyDragInstruction()`（指示確定、guard/chase/goto/コピーいずれも1回）／`applyEventsForTick()`の`attack`ケース（攻撃）／同`push`・`pull`・`linkedAttack`・`heal`・`poison`・`musouRecover`・`marchBoost`・`trapSet`の各ケース（スキル発動、全て共通の`skill`音）／`showMatchResultPopup()`（win/lose/drawをそのままキーとして流用）。
  - **検証方法**: `AudioContext.prototype.createOscillator`/`createBufferSource`をブラウザ上で一時的にラップして呼び出し回数を計測し、各アクション後の増分が各プリセットの音数（例: buyは2音→+2、attackは1音+ノイズ1回→osc+1/noise+1、winは4音→+4）と正確に一致することを実機（Chrome拡張、ローカルサーバ）で確認。指示ドラッグはpointerdown/pointermove/pointerupを実際にdispatchしてguard指示を確定させ、+1音を確認。skipToEnd()（最後まで飛ばす機能）はSEを含む演出全般を元々スキップする設計のため、SEもtickごとの通常再生時のみ鳴る（意図通り）
- **[2026-09-19] マップ機能を追加（複数マップ対応）**。UC要望「MAPを追加したい」→「ロビーでホストが選ぶ」「レイアウトは自分で指定する」との回答を受け、UCが文章で指定したレイアウト（「左右レーンに差があるように。右は壁少なく遠距離型有利、左は壁多く近距離型有利、中央は無しで左右を分断する壁」）を実装。
  - `MAPS`レジストリ（`classic`＝既存マップ／`laneSplit`＝新マップ）を新設。壁生成をマップごとの`build()`関数に切り出し、`currentMapId`と`setMap(mapId)`で切替可能にした
  - **ミラー方式の落とし穴**: classicの180度点対称ミラー（col,row両方反転）をそのまま新マップにも使うと、「左レーンだけ壁多め」の非対称パターンがP2側では左右反転して適用されてしまい、両陣営で「左＝近接有利」を揃えられないことに設計段階で気づいた。新マップだけ行のみ反転するミラー（col不変・row→ROWS-1-row）に変更して解決（詳細は設計書§2-1）
  - **接続性の事前検証**: 新しい壁レイアウト（中立地帯col9・10を全行封鎖）が孤立エリアを生まないか、実装前にNode.jsで簡易BFSスクリプトを書いて検証（全342マスが1つの連結成分であること、各拠点周辺の自陣候補マスが十分にあることを確認）してから組み込んだ
  - ロビーの「ホストになる」欄に`<select id="mapSelect">`を追加。`createRoom()`で選択値を`rooms/{code}/mapId`に書き込み、`handleRoomUpdate()`で全クライアント（ホスト自身の再読込含む）が`setMap(data.mapId)`で同期する。ローカルテスト（`devSkipLobbyBtn`）でも同じセレクトの値を反映
  - **踏んだバグ（実装中に自己発見・修正）**: 盤面のヘックスセル生成（色・クリック可能ヒットボックスの`hex-clickable`要素）が、以前はページ読み込み時に一度だけ実行されるトップレベルコードだった。`setMap()`で`walls`（Setオブジェクト）を差し替えても、既に生成済みのSVG要素は古いマップの見た目・当たり判定のまま変わらず、ゲスト側では壁が透明な「空きマスに見えるが配置できない」逆に「壁に見えないのに実は開いている」マスが発生する不具合になっていた。生成処理を`buildBoardCells()`として関数化し、`setMap()`から呼び直すことで解消。同様に、マップ名表示ラベルも`render()`頼みだと非同期の`handleRoomUpdate()`経由の切替に追従しないタイミング問題があり、`setMap()`内で直接更新するよう修正した。**教訓: 「一度だけ実行されるトップレベル初期化コード」は、後から動的に切り替え可能にする機能を追加する際に見落としやすい典型パターン。実機で2タブ（ホスト/ゲスト）を使い、見た目のラベルだけでなく実際のクリック判定（`querySelector('[data-col][data-row]')`の有無）まで確認したことで発見できた**
  - 実機検証（Chrome拡張、2タブ）: ホストがレーン分断を選択→`rooms/{code}.mapId`にFirebase REST APIで直接確認→ゲスト参加時に壁の当たり判定・色・マップ名ラベルが即座に正しく反映されること、クラシックマップ（デフォルト）が従来通り動作すること（回帰なし）、両マップで戦闘が正常に完走することを確認。コンソールエラーなし
- **[2026-09-19] AFBElist.xlsx更新分でキャラ6体・スキル3種を追加**。UC「AFBElistみてキャラ追加したー」（初回は保存し忘れで前回同様の内容だったため、保存し直してもらってから再取得）。新規: タンク(なし)・忍者(変化)・侍(追従2)・バード(身軽+行進曲)・クラフター(建設)・フェンサー(早業+移動ボーナス1)。画像は前回セッションで「まだ検討中」用にUCが既に用意済みだったファイル（elephant_tank/tanuki_ninja/shiba_samurai/parrot_bard/beaver_crafter/leopard_fencer）をそのまま使用でき、今回は画像不足なし。
  - **N値・仕様の未確定点はUC確認してから実装**: 侍の追従N→2、フェンサーの移動ボーナスN→1、タヌキ「変化」の変装先決定方法→配置時にランダムで1体選ぶ、ビーバー「建設」の壁の寿命→設置から5tick経過で消滅・ラウンドを跨いでもtickカウント継続、ヒョウ「早業」の優先度→「守る」以外の目的の時だけ最優先（守るを選べば従来通り最後）。いずれもAskUserQuestionで選択肢提示し即答してもらえた
  - **変化（忍者等）の実装**: `unit.disguiseAs`（配置時にランダム選出した自分の他ユニットのcharId）・`unit.revealed`（攻撃を受けたら`applyDamage()`内でtrueにし、`reveal`イベントをログに残す）をユニットに追加。`initBattleState()`・`advanceToNextRound()`のユニット再構築（どちらも明示的なフィールドのホワイトリスト方式だったため両方に追記が必要だった）・`interpretRoundOutcome()`が使う`applyEventHeadless()`（`reveal`ケース追加）の3箇所全てで引き継がないと次ラウンドで判明状態が消えてしまうことに実装中に気づいた。表示側は配置画面のマーカー描画・タップ時のステータスパネルの2箇所で「相手から見た時だけdisguiseAsのキャラとして描画する`displayChar`変数」を使うよう変更（自分から見る時・戦闘フェイズ中の再生は常に素の姿のまま＝秘匿ルールは配置フェイズのみが対象という解釈。UCには確認していないが、指示秘匿など既存の秘匿ルールも配置フェイズ限定のため整合的と判断）
  - **建設（クラフター）の実装**: マップ固定壁とは別に`tempWalls`（key→残りtick数のMap）をモジュールレベルに新設し、`isWall()`がこれも参照するようにした。`performAction()`の移動時に通過マスへ`tempWalls.set(key,5)`、tickループの先頭で全エントリを毎tick1ずつ減らし0で削除。**ラウンドを跨いでも`tempWalls`自体をリセットしない**ことで「tickカウント継続」というUC仕様をそのまま満たせる（`persistentTraps`のような別変数へのコピーが不要）。配置画面の見た目・当たり判定を毎ラウンド開始時に反映するため`advanceToNextRound()`から`buildBoardCells()`を呼ぶよう追加（マップ切替バグ修正時に用意した関数を流用）。試合リセット時は`tempWalls = new Map()`で明示的にクリア
  - **早業（フェンサー）の実装**: `simulateBattle()`のtickシャッフルを「守る以外→守る」の2段階から「早業(守る以外)→通常(守る以外)→守る」の3段階に拡張
  - **検証方法**: Node.jsに切り出したテストハーネスで、早業の行動順・建設の壁の生成/5tick消滅/ラウンドを跨いだtick継続・変化のreveal発火とinterpretRoundOutcome経由での引き継ぎを、それぞれ期待値付きで確認。実機（Chrome拡張）ではデッキ編集で全6体の画像・価格・スキル説明文（N値の埋め込み込み）を確認した後、実際にタヌキを配置してP2視点でタップ→偽装先キャラの見た目・ステータスで表示されること、P1視点（本人）では常に真の姿が見えることを確認。コンソールエラーなし
  - **[2026-09-19追記] 実機で建設・早業も戦闘中に確認**（UC「実機で動作確認して」への対応）。前回はNode.jsのみだった2件を、devSkipLobbyBtn（ロビーをスキップしてローカルでテスト）でP1にクラフター・フェンサー・アーチャーを配置しP2にタンクを配置、盤面クリックをJS経由でディスパッチして実際に戦闘を実行して確認: ①t1〜t5の毎tickでログが「フェンサー(P1)移動」→「アーチャー(P1)移動」→「クラフター(P1)移動」の順に並び、早業持ちが非guard・非早業の味方より確実に先に行動することを実戦ログで確認。②クラフターの移動と同じtickに「建設発動」ログが毎回出現することを確認。**訂正（2026-09-20）**: このとき盤面に見えていた破線の青枠ヘックスは一時壁の専用表示ではなく、たまたま同じマスに重なっていた攻撃範囲オーバーレイ（`gAttackRange`、`renderAttackRangeOverlay()`）だった。一時壁専用の表示は当時まだ存在せず、下記2026-09-20の対応で新規実装した
- **[2026-09-20] 建設の一時壁に専用マーカー（残りtickのカウントダウン表示）を追加**。UC要望「建設したときに専用の壁表示をして5,4,3,2,1って感じで変るものを」。
  - **課題**: `tempWalls`（実装値の正典）は`enterBattleMode()`内で`simulateBattle()`を呼んだ瞬間にそのラウンドの末尾まで一気に計算し尽くしてしまうため、tick単位で巻き戻し・早送りが自由な戦闘再生（`stepForward()`/`restartPlayback()`/`skipToEnd()`）の途中経過の値をそこから直接読むことができない（変化のreveal・disguiseAsと同じ「ログ・事前計算値だけを頼りに再生する」既存方針の対象が1つ増えた形）
  - **実装**: `enterBattleMode()`で`simulateBattle()`を呼ぶ**直前**に`wallSnapshotAtRoundStart`（＝このラウンド開始時点の`tempWalls`のコピー、前ラウンドからの持ち越し分）を確保。その後`battleResult.log`から`wallStatesByTick[tick]`（そのtick終了時点でアクティブな一時壁のMap）を、実際の`simulateBattle()`内の減衰ロジック（毎tick先頭で全エントリ-1・0で削除→`wallBuilt`イベントのマスは5にセット）と全く同じ手順でtick0から順に再現して事前計算。`restartPlayback()`（tick0）・`stepForward()`（毎tick）・`skipToEnd()`（最終tick）それぞれの末尾で`renderTempWallMarkers(wallStatesByTick[tick])`を呼び、新設した`#gTempWalls`層（琥珀色の破線枠ヘックス＋残りtick数の白文字、マップ固定壁の黒塗り・攻撃範囲の破線枠オーバーレイとは別レイヤーで視覚的に混同しない配色にした）を該当tickの状態に同期させる。戦闘画面を抜けたら（`exitBattleMode()`）マーカーをクリア（配置画面では壁マス自体の色で十分なため）
  - **実機検証**（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）: クラフターが5マス移動する行程を実際に戦闘再生し、①1tickずつ進める再生で直近5マスの壁がそれぞれ独立したカウントダウン（5→4→3→2→1→消滅）を表示すること、②最初まで巻き戻すとtick0時点（このラウンド開始時点で持ち越しがなければ0件）に正しく戻ること、③最後まで飛ばす（`skipToEnd()`）でも同じ最終状態に一致することを確認。次ラウンドへ進んだ際に`#gTempWalls`のマーカーが正しく消え、配置画面の壁マス表示（`buildBoardCells()`）に一本化されることも確認。コンソールエラー0件
- **[2026-09-20] デッキ編成画面を2カラム構成に変更（選択中12体を左に常時リスト表示）**。UC指摘「デッキ編成の少し見づらい、12枠しっかりみえるようにしたい」。まずArtifact（Design canvasタイプ）で3案のモックアップを作成しUCに提示: 案A=ヘッダー直下に固定12枠パネル、案B=左に選択中12体の常時縦リスト＋右に一覧グリッドの2カラム、案C=一覧グリッド内で選択済みを上段固定（配置画面の「配置待ち/配置済み」分割の踏襲）。**UCが案Bを選択**。
  - `.deck-edit-modal`の幅を`min(760px,100%)`→`min(920px,100%)`に拡大。モーダル本体に`.deck-edit-body`（flex行）を新設し、左に`#deckEditSelectedList`（幅220px固定・独立スクロール・選択中キャラを番号付きで縦に並べる。行クリックで`toggleDeckCharacter()`＝選択解除、グリッド側カードと同じ挙動）、右に既存の`.shop-grid`（一覧グリッド、独立スクロール）を配置
  - 前回セッションで追加した横並びアイコン列（`renderDeckEditSelectedRow()`/`#deckEditSelectedRow`）は今回の2カラム化で不要になったため置き換え。番号・キャラ画像・名前・✕（削除）を1行に並べた`renderDeckEditSelectedList()`に差し替え
  - 実機検証（Chrome拡張、ローカルサーバ）: 選択中12体が実際のキャラ立ち絵付きで一目で見えること、リスト行クリック→デッキから外れる→即座に一覧グリッド側の該当カードが選べる状態に戻ること、逆に一覧グリッドでキャラをクリック→リストに追加されること、`保存`は未クリックなので実際のデッキ1データには影響がないことを確認。コンソールエラー0件
- **[2026-09-20] 移動ボーナス1が実質ノーオペだったバグを修正（狩人・フェンサーとも移動ボーナス2に変更）**。UC指摘「移動ボーナス1ってこれどうなってる？文字だけ読むと意味ないスキルになってる気がする」。
  - **原因**: `performAction()`のmove処理で`const moveRange = moveBonusSkill ? parseInt(...) : 1;`となっており、スキル無しキャラのデフォルト移動もそもそも「1AP消費で1マス」。つまりN=1は既定値と完全に同一で、`移動ボーナス1`は見た目上のスキル表記だけで何の効果も無かった（`drawApScaledPolyline()`の指示線プレビュー計算も同じ式を使っており、そちらも同様に無効化されていた）。フェンサーのN値は前回セッションでAskUserQuestion提示時に自分（Claude）が「移動ボーナス1（推奨）」と提案してそのまま採用されたもので、狩人はAFBElist.xlsx由来
  - **対応**: UCにAskUserQuestionで確認し「Nを2以上に引き上げる」を選択。狩人・フェンサーとも`移動ボーナス2`に変更（`CHARACTERS`配列・設計書§9の該当2行）
  - **検証方法**: Node.jsテストハーネスで、開けた場所に置いた狩人・フェンサーを"goto"で直進させ、1tick目の`move`イベントの`from`→`to`をゲーム内の`hexDistance()`（ヘックス距離、単純なcol/row差分ではなく正しいオフセット座標のヘックス距離）で検証。狩人・フェンサーともに移動距離2・消費AP1、比較対象のスキル無しソルジャーは移動距離1・消費AP1であることを確認（計5アサーション全成功）。実機（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）でもフェンサーとソルジャーを同じ列に配置し実際に1tick進めて確認: フェンサーはrow4→row6（2マス）、ソルジャーはrow4→row5（1マス）と、狙い通りの差が実戦でも再現された。コンソールエラー0件
- **[2026-09-20] ラウンドごとの購入可能プールを「毎回独立抽選」から「前ラウンドのプール＋2体」の累積方式に変更**。UC要望「前のラウンドででたキャラ＋２体を出すようなかんじにしてほしい」。
  - **背景**: 従来の`regenerateRoundPool(round, seed)`は毎ラウンド`pickRandomPool()`でデッキから**独立に**6/8/10/12体を抽選し直しており、R1で買えたキャラがR2の抽選で漏れて消えることがあった（体数自体は6→8→10→12と+2ずつ増えていたが、中身は無関係だった）
  - **実装**: `regenerateRoundPool()`をR1（または`currentPool`が空）の時だけ従来通り新規抽選、R2以降は「現在の`currentPool`をそのまま残し、デッキの残り（`currentPool`に無いid）から`targetSize - currentPool.length`体（＝常に2体）だけ`pickRandomPool()`で追加抽選し結合」する方式に変更。`ROUND_POOL_SIZE`の6/8/10/12という体数設定自体は変更していない（元々+2ずつだったため、累積方式にしても体数の帳尻は自動的に合う）
  - **検証方法**: Node.jsテストハーネスで12体デッキを与え、R1〜R4を順に呼び出し、各ラウンドのプールが前ラウンドの完全な上位集合（isSuperset）であること・新規追加が常にちょうど2体であることを確認。実機（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）でも実際に4ラウンド分バトル→次ラウンドへ進行を繰り返し、R1(6)→R2(8)→R3(10)→R4(12、デッキ全体)といずれも前ラウンドの内容を完全に含んだまま2体ずつ増えることを目視確認。コンソールエラー0件
- **[2026-09-20] 拠点を1マス後退＋レーン分断MAPの左側中立地帯に壁を追加**。UC要望「両方のMAPとも拠点を一つ後ろに下げる。左右差ありのMAP方はさらに左側の中立地帯に壁をいっぱい用意してほしい」。
  - **拠点後退**: `FIXED_BASES`（両マップ共通の定数）をP1: row3→row2、P2: row15→row16に変更（180度対称維持）。既存の壁レイアウト（`WALLS_HALF`・`MAP2_WALLS_TOP`）のどちらとも新しい拠点位置は衝突しないことを確認済み
  - **レーン分断MAPの左側中立地帯**: `MAP2_WALLS_TOP`に中央分断壁(col9/10)より左(col0-8)の互い違いの壁を追加（row7: col2,4,6,8／row8: col1,3,5,7／row9: col2,4,6,8）。**踏んだバグ**: 最初col0も含めて3行(7,8,9)とも塞いだところ、盤面端の(0,8)/(0,10)マスが周囲3方向全て壁に囲まれて孤立するのをNode BFSで発見（盤面端は隣接マスが少ないため、中央付近なら平気な間引き方でも端では詰みうる典型パターン）。col0を各行から除外して解消
  - **検証方法**: Node.jsでBFS接続性チェック（両マップとも全マス1つの連結成分・4拠点すべて到達可能であることを再検証）。実機（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）で両マップの盤面を目視確認: 拠点が両陣営ともrow2/16相当の位置に1マス下がって表示されること、レーン分断MAPで左側中立地帯に市松模様状の壁ブロックが追加され右側は従来通り疎らなままであることを確認。ランサーを配置して新しい壁エリア方向へ「指定マスへ向かう」を設定し実際に戦闘を再生してもエラーなく完走することも確認。コンソールエラー0件
- **[2026-09-20] バグ修正: 変化（忍者）が戦闘中は機能していなかった**。UC報告「タヌキの変化がうまくいってない」。
  - **原因**: 配置画面（`render()`のマーカー描画・タップ時のステータスパネル）は`disguiseAs`/`revealed`を正しく見て偽装表示していたが、**戦闘再生側（`createBattleMarker()`のラベル・キャラ画像、`entityLabelHtml()`のバトルログ名前表示）が両方とも`entity.charId`を直接参照しており、`disguiseAs`/`revealed`を一切見ていなかった**。さらに`applyEventsForTick()`の`case "reveal"`が`entity.revealed`を更新すらしていなかった（コメントに「戦闘中は元々素の姿で表示済み」とあり、秘匿は配置フェイズのみが対象という前回セッションの独自解釈のまま実装されていた）。結果、開戦した瞬間に相手のタヌキ（忍者）の本当の姿が盤面・ログの両方に即座に表示されてしまい、変化スキルが実質無意味になっていた
  - **対応**: 配置画面と同じ判定条件（`!isOwn && disguiseAs && !revealed`）を`battleDisplayCharId(entity)`という共通関数に切り出し、`createBattleMarker()`・`entityLabelHtml()`から参照。`reveal`イベント処理で`entity.revealed = true`を設定した上で新設の`updateBattleMarkerIdentity(entity)`を呼び、マーカーのラベル・画像をその場で偽装先→本当の姿に切り替える。`skipToEnd()`（最後まで飛ばす）でも最終状態のマーカー表示に反映されるよう同関数を追加呼び出し
  - **既知の制約（実装上受容）**: `skipToEnd()`のバトルログは、`displayState.units`を最終状態にセットしてから全ログ行を一括生成する既存の仕組み上、正体判明**前**のtickの行も判明**後**の状態（＝本当の名前）で表示されてしまう（1tickずつ再生する場合は正しいタイミングで切り替わる）。マーカー表示（盤面上の見た目）は`skipToEnd()`でも正しく最終状態を反映する
  - **検証方法**: 実機（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）で、P2に忍者＋ランサーを配置（忍者はランサーに変装）、P1視点で戦闘を1tickずつ再生。①正体判明前（t1〜t6）はバトルログ・盤面マーカーとも「ランサー(P2)」と表示され本物のランサーと見分けがつかないこと、②実際に攻撃を受けたt7で「正体判明 忍者(P2)」のログが出た直後から、③t8以降のログ・盤面マーカーとも「忍者(P2)」に切り替わることを確認。コンソールエラー0件
- **[2026-09-20] バランス調整4件**。UC指示「フェンサーの行動力を10まで落として、建設10tickに使用、吟遊詩人に追従スキルを付与、バードの行動力を15に」。「建設10tickに使用」の意図と追従NのN値はAskUserQuestionで確認済み（建設: 5tick→10tickへの延長／吟遊詩人の追従N: N=2、侍と同じ）。
  - フェンサー行動力16→10。建設の壁持続時間5tick→10tick（`WALL_BUILD_DURATION`定数を新設し、`performAction()`の設置箇所と`wallStatesByTick`事前計算の両方から参照するよう統一。**踏んだ気づき**: 前回セッションで一時壁の残りtick表示機能を実装した際、「5」を2箇所に別々にハードコードしていたため、今回の変更で1箇所直し忘れる余地があった。定数化して解消）。吟遊詩人に追従2を追加（3スキル目。毒使いに続き「基本2つまで」の2例目の例外、設計書§3に明記）。バード（パロットバード）行動力12→15
  - **検証方法**: Node.jsでCHARACTERS配列の数値・スキルを直接確認、`WALL_BUILD_DURATION`が実際に10であること、建設で設置した壁が9tick後もまだ残っており（remain=3等、10-経過tick数と一致）10tick目で消滅する減衰カーブが正しいことを確認。実機（Chrome拡張、ローカルサーバ）でデッキ編成画面の該当4体のスキルpill・ツールチップ・行動力の数値表示が更新後の値を正しく反映していることを確認。コンソールエラー0件
- **[2026-09-20] AFBElist.xlsx更新分でキャラ7体・スキル6種を追加（「まだ検討中」だった全キャラが実装完了）**。UC「AFBElist.xlsx キャラ追加したー　これ読んで追加して」。前回セッション時点で「まだ検討中」として保留していた7体（キリン・ウマ・レッサーパンダ・サル・カンガルー・サイ・カラス）について、UCがスキル一覧シートに効果を追記したため実装。新id: boxer(ボクサー)・gladiator(グラディエーター)・priest(僧侶)・carrier(キャリアー)・merchant(マーチャント)・cleaner(クリーナー)・scholar(学者)。画像は前回セッションでUCが用意済みだったファイル（kangaroo_boxer/rhino_gladiator/giraffe_monk/horse_carrier/monkey_merchant/crow_cleaner/redpanda_scholar、全身・顔とも）がそのまま使え、今回は不足なし。
  - **仕様確認**: 新スキル6種のうち4件（ワンツー・商売・牽引・解析）は文面だけでは実装方法が一意に決まらなかったためAskUserQuestionで確認。①ワンツー（攻撃時2回攻撃）→**2発とも独立した「攻撃」として発動**（毒・押し込み等の付随効果もそれぞれ判定、行動力消費は通常の1回分のまま）。②商売（攻撃ごとに次ラウンドのクレジット+20）→**次の1ラウンド限りの一時ボーナス**（永続増加ではない）。③牽引（移動時、攻撃範囲内の味方1体を一緒に動かす）→**範囲内からランダムに1体選び、移動先の隣接マスへ対象のAP消費なしで移動**。④解析（射程ボーナスN持ちからの被ダメージ半減、「5以下にはならない」）→**半減後のダメージの下限を5にする**。突進（血の匂いの拠点版）と死肉あさり（範囲内の敵死亡でAP+1/HP+20回復、自分のみ）は文面から一意に読み取れたためUC確認なしで実装
  - **実装箇所**: `computeGoal()`に突進の分岐追加（血の匂いと同じ`nearestByHexDistance()`を拠点に適用するだけ）。`resolveAttack()`に解析の被ダメージ半減（射程ボーナスN持ちが攻撃者、かつ対象の陣営に解析持ちが対象を射程内に捉えていれば`Math.max(5, Math.floor(dmg/2))`）。`applyDamage()`の死亡分岐に死肉あさりのスキャベンジ処理（倒れたキャラを射程に収めている敵陣営の死肉あさり持ち全員が独立に発動）。`performAction()`の攻撃分岐にワンツー（2回目の`resolveAttack()`呼び出し、対象が生きていれば）と商売（`unit.merchantBonusStacks`を攻撃のたびに+1）。同じく移動分岐に牽引（移動前の攻撃範囲から味方をランダムに1体選び、移動後に隣接マスへ無償で移動。**踏んだバグ**: `isOccupied()`が対象自身のIDを除外して判定するため、対象がすでにいるマスも「空きマス」として誤検出され、実質何も動いていないのに`towMove`イベントが記録される不具合が発生 → 移動先が対象の現在地と一致する場合は何もしない・ログも出さないよう修正）。`advanceToNextRound()`に商売のクレジット加算（`outcome.units`の`merchantBonusStacks`合計×20を該当プレイヤーのクレジットに追加）。`applyEventHeadless()`に新イベント3種（towMove/scavenge/merchantBonus）のケースを追加（即時終了時のラウンド結果再構築で必要、既存のfollowMove/musouRecover等と同じ理由）
  - **検証方法**: Node.jsテストハーネスで6スキル全てを個別シナリオで検証（ワンツー: 同一tick内に独立した2件のdamageイベント＋行動力消費は1回分のまま／突進: guard指示でも移動し敵拠点との距離が縮む／牽引: towMoveイベント発生かつ対象のAP不変／商売: merchantBonusイベント数とmerchantBonusStacksの一致／死肉あさり: 範囲内で敵撃破時にscavengeイベント発生／解析: 射程ボーナス2持ちからの攻撃ダメージが期待通り半減）。実機（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）でデッキ編成画面の全7体の画像読み込み・スキルpill表示を確認した後、グラディエーター（突進）とキャリアー＋足軽（牽引）を実際に配置して戦闘再生し、グラディエーターがguard指示のまま敵拠点へ突撃し続けること、キャリアーの移動のたびに範囲内の味方（時にはグラディエーター自身）を無償で牽引することをログ上で確認。コンソールエラー0件
- **[2026-09-20] 連携攻撃の仕様変更: 行動力0のキャラは発動しない**。UC指示「連携攻撃の仕様変更　行動力がなくなったら、連携攻撃は発動しない」。`triggerLinkedAttacks()`は元々`ally.alive`しかチェックしておらず、行動力を使い切ったキャラでも近接/遠距離連携攻撃が無制限に発動していた。ループ先頭に`if (ally.ap <= 0) continue;`を追加。連携攻撃自体は行動力を消費しない仕様のまま（発動条件にAP残量が要るようになっただけ）。Node.jsで行動力0のランサー（近接連携攻撃持ち）が発動しないこと・行動力が十分な場合は従来通り発動すること（回帰確認）の両方を確認。コンソールエラー0件（DOM非依存のロジック変更のため実機確認は省略）
- **[2026-09-20] 建設の壁を2件修正: 配置画面の見た目を専用色に／クラフターが自分の壁に埋まって動かせなくなるバグ**。UC報告「配置画面の建設で作った壁が普通の壁にみえるからなおして、戦闘中と同じように、あとクラフターが壁に埋まり、うごおかせなくなる」。
  - **見た目の修正**: `buildBoardCells()`が`isWall()`（固定壁・一時壁の両方を含む判定）だけで色分けしていたため区別がつかなかった。`walls.has(k)`（固定壁）と`tempWalls.has(k)`（一時壁）を別々に判定し、一時壁は戦闘再生中の一時壁マーカー（`#gTempWalls`）と同じ`var(--temp-wall)`系の配色・破線にした上で、残りtick数のテキストも配置画面上に表示するようにした
  - **埋まるバグの原因**: `performAction()`の建設処理が`traversed`（今回の移動で通過した全マス、**今まさに到達した現在地を含む**）をそのまま壁化していた。クラフターは移動ボーナスを持たず常に1マスずつしか動かないため、`traversed`は毎回「今立っている1マスのみ」になり、**移動するたびに自分の現在地そのものを壁にしていた**。`isWall()`がtrueのマスは`buildBoardCells()`でクリック可能な当たり判定（`hex-clickable`）自体を生成しないため、次ラウンドの配置画面でそのクラフターをクリックして選択・移動することが一切できなくなっていた
  - **修正**: 壁にするマスを「今回移動する前にいたマス（`fromPos`）＋今回通過した経由点（到達先を除く）」に変更（`[fromPos, ...traversed.slice(0, -1)]`）。到達先＝今の現在地は二度と壁化しない
  - **検証方法**: Node.jsで、クラフターが複数tickにわたって移動する一連のシナリオを検証し、①どのwallBuiltイベントもその瞬間のクラフターの現在地と一致しないこと、②戦闘終了時点のクラフターの最終位置が`isWall()`でfalseのままであること、③壁の生成自体は引き続き機能していることを確認。実機（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）でクラフターに5マス分の移動指示を出して実際に戦闘→次ラウンドへ進行し、盤面上に琥珀色破線＋残りtick数（5〜9）の壁が経路上に並ぶこと、クラフター自身がいる到達先マスだけは壁化されておらず実際にクリックして選択（ステータスパネル表示）できることを確認。コンソールエラー0件
- **[2026-09-20] 解析の発動を可視化（ログカード＋盤面ラベル追加）**。UC「解析ってうまく作動している？」→Node.jsで5パターン（射程ボーナス持ちからの半減／射程ボーナス無しは半減なし／範囲外は半減なし／下限5の切り上げ／味方の攻撃には無関係）を検証しロジック自体は正常と回答したところ、UC「追加して」で発動演出を要望。実装時は時間の都合で他スキルにあるようなログカード・ラベル表示を意図的に省略していたため、被ダメージが小さくなるだけで発動が見た目に分からなかった。
  - `resolveAttack()`の解析判定を`.some()`から`.find()`に変更し、実際に軽減した学者ユニットを特定できるようにした上で`{ type: "analysis", unitId: 学者のid, targetId: 軽減された味方のid }`をログに追加（盤面状態は変えないイベントなのでapplyEventHeadless側の対応は不要、linkedAttack等と同じ扱い）。`EVENT_LABELS`・`SKILL_CARD_NAME`に`analysis`を追加してバトルログに「解析発動」の肖像カードが出るようにし、`applyEventsForTick()`に軽減された味方の位置へ`spawnSkillLabel(..., "解析")`を出すcaseを追加
  - **検証方法**: 実機（Chrome拡張、ローカルサーバ、devSkipLobbyBtn）で学者＋クレリックを配置し、射程ボーナス2持ちのアーチャーにクレリックを攻撃させ、①バトルログに学者(P1)の「解析発動」カードが攻撃の直前に表示されること、②直後の被ダメージが半減された値（アーチャー攻撃力25→12）になっていることを確認。コンソールエラー0件
- **[2026-09-20] バランス調整6件**。UC指示「あと端数は切捨てで、最小５単位　クリーナーの射程ボーナス2に変更　僧侶の全体回復を２０に変更　全体回復の仕様変更　３消費するたびに変更　マーチャントの価格を４０に変更　忍者のスキルを変化から身軽、価格を６０に変更」。
  - 「端数は切捨てで、最小５単位」は直前に実装した解析（`Math.max(5, Math.floor(dmg/2))`）の確認で、コード変更は無し
  - クリーナー射程ボーナス1→2／僧侶全体回復10→20／マーチャント値段65→40／**忍者のスキルを変化→身軽に変更、値段75→60**（変化の実装自体はコードに残したまま。現時点でこの効果を持つキャラは存在しなくなった）
  - **全体回復Nのグローバル仕様変更**: `triggerHealAura()`のトリガー間隔を「行動力5消費毎」→「3消費毎」に変更（`Math.floor(unit.apSpentTotal / 5)`→`/ 3`）。全体回復Nを持つ全キャラ（クレリック・僧侶）に影響
  - **検証方法**: Node.jsでCHARACTERS配列の数値・スキルを直接確認。全体回復のトリガー間隔変更は、負傷した味方がいる状態でクレリックに1AP消費の行動を繰り返させ、3AP消費目（従来なら5AP消費目）のtickでhealイベントが発生することを確認。実機（Chrome拡張、ローカルサーバ）でデッキ編成画面の該当4体の値段・スキルpill表示が更新後の値を正しく反映していることを確認。コンソールエラー0件
- 残る未確定事項は設計書 §10（#3〜#6）、バランス懸念は §11 を参照
